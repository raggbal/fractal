# Generator Failures (project-local)

> `reviewer` agent が発見した generator の実装失敗パターンを蓄積する。
>
> 以降の **generator / reviewer** のプロンプトに**自動注入**される。
>
> 書き手: `reviewer` agent（人間も追記可）
> 読み手（自動注入）: generator, reviewer

## 記法

追記のみ（非破壊）。各エントリは:

> このファイルは**自動注入**されるため 40k 文字上限がある。超えそうになったら最古スプリントのエントリを
> `.claude/rules-archive/generator_failures.archive.md`（`rules/` 外＝非注入）へ退避する。過去の教訓を grep する際は archive も対象に含めること。


```markdown
## YYYY-MM-DD [sprint-id] 失敗分類
- 現象: 何が起きたか
- 原因: なぜ起きたか
- 回避策: 次回どう防ぐか
```

## 失敗パターン

## 2026-08-10 [20260809-031217-notetree-file-dnd] 既存の正典 pure 関数（extractFirstH1）があるのに inline 正規表現で H1 抽出を再実装し `# C#` 切り捨てバグを 3 度目に再発（同一 sprint 内の隣接コードが正典を使っているのに新設 3 関数が揃って inline）
- 現象: 再オープン⑤の新設 3 関数（importOutPageNodeToMd / linkMdSubpageToMd / importMdSubpageIntoOut）がすべて `content.match(/^\s{0,3}#\s+(.+?)\s*#*\s*$/m)` の inline 正規表現で H1 タイトルを抽出。この形（末尾 `#` 列を空白前置なしで貪欲に剥がす）は `# C#` → `C`・`# F# and C#` → `F# and C` と末尾記号タイトルを silent に切り捨てる。CommonMark 準拠の正典 `extractFirstH1`（src/shared/md-h1-utils.ts:42 — 閉じ記号は `[ \t]+#+$` = 空白前置必須でのみ剥がす）が存在し、同ファイルの隣接機能（resolveSubpageTitle 経由）はそれを使っていた。同一バグクラスは 2026-07-19（generator）・2026-07-23（designer archive）に記録済みで **3 回目の再発**。新規 TC（TC-CN-02..05）は dispatch 検証のみでタイトル内容を踏まず full-green ですり抜けた
- 原因: 「H1 を 1 個取るだけ」の小さな処理を、既存正典を grep せずその場で書いた。3 関数が同時に新設されたため 1 つの inline が 3 箇所に複製された。失敗 DB に「buggy シグネチャ = `\s*#*\s*$` は赤信号」と記録済みだったが、新機能実装の文脈では失敗 DB を「H1 抽出」で引かなかった
- 回避策: (a) **md から title/H1 を取る処理は必ず `extractFirstH1`（md-h1-utils）か `resolveSubpageTitle`（md-subpage-utils）を import する** — `content.match(/^#/m)` 系の inline を書いた時点で失敗 DB 該当（`\s*#*\s*$` は 3 回実測済みの赤信号シグネチャ）。(b) reviewer は diff 内の新規 `match(/^\s*\{0,3\}#/` 系正規表現を grep で検出し、正典関数への置換を code_fix で差し戻す。(c) title 抽出を含む TC は `# C#` を必ず 1 本踏む（TC-CN-09 が本 sprint の番人）


## 2026-08-09 [20260809-031217-notetree-file-dnd] 新設 host メソッドの sidepanel 委譲層（SidePanelHostBridge 手書きクラス）への個別追加を落とし、番人 TC が「任意メソッド名に応答する Proxy fake host」だったため実クラスの欠落を検出できず false-green
- 現象: FR-TF-06a（tree file → md editor D&D、main/sidepanel 両対応）で、editor.js の drop 分岐は `typeof targetHost.attachTreeFileToMd === 'function'` ガード付きで呼ぶ設計どおり実装され、host 3 層（bridge / handler case / provider 実装）も sidePanelFilePath 対応込みで配線済みだったが、sidepanel の targetHost である **SidePanelHostBridge（editor.js:2-180 の手書き委譲クラス）に attachTreeFileToMd の個別追加が漏れた**。→ sidepanel md への drop は typeof ガードが false → silent no-op（FR 違反）。editor.js:137 の既存コメント自身が「SidePanelHostBridge は共有 factory 非経由の手書きクラスなので個別追加が必要（designer_failures 2026-07-20）」と警告済みだった。番人 TC-WV-09 は「main/sidepanel の targetHost 選択を両方踏む」と宣言していたが、EditorInstance.instances に **`new Proxy` の recorder fake host を直接 push**（存在しないメソッド名でも関数を返して記録する）していたため、実クラスのメソッド欠落を素通しして green。TC-RG-01 の 4 層配線 wiring guard（webview 呼び出し元→bridge→handler→provider）も sidepanel 委譲層 = 第 5 層を数えないため検出不能
- 原因: (1) 「main/sidepanel 両対応」の実装を drop 分岐（呼び出し側）+ host 3 層で完了とみなし、**sidepanel だけに存在する委譲層 1 枚**（SidePanelHostBridge の手書きメソッド）を配線層として数えなかった。既存コード内の「個別追加が必要」警告コメントを新メソッド追加時に参照しなかった。(2) 番人 TC のテストダブルに「全メソッドに応答する Proxy」を使ったため、typeof ガード付き呼び出しの検証が**実装の有無と独立に必ず通る**（tautology）。typeof ガードは「無ければ静かに何もしない」ための防御なので、Proxy fake はそのガードの発火を構造的に検証できない
- 回避策: (a) **md editor の host メソッドを新設したら `grep -n "class SidePanelHostBridge" src/webview/editor.js` の委譲クラスに個別メソッドを必ず追加する**（linkMdAsSubpage :74-79 が precedent。共有 factory 非経由なので自動では生えない）。「main/sidepanel 両対応」FR の配線層は 4 層でなく **5 層**（webview 呼び出し元 → sidepanel 委譲 → bridge → handler → provider）で数える。(b) **typeof ガード付き呼び出しの番人 TC に「任意メソッド名に応答する Proxy fake」を使わない**。実クラス（window 公開済みの SidePanelHostBridge 等）に recorder を包ませて経由させるか、fake は実クラスと同じメソッド集合だけ持つ明示オブジェクトにする（Proxy fake は「メソッドが存在しないと発火しない」防御の欠落を検出できない）。(c) wiring guard（N 層配線の grep 数え）を書くときは、面ごとに層数が違わないか（sidepanel だけ +1 層）を確認して guard の層リストに含める

## 2026-08-06 [20260806-133523-outliner-shift-enter-linebreak] 同一 model 値（node.text）を消費する複数の sink のうち「代表 1 sink（複数選択 copy）」だけに \n 潰しを適用し、兄弟 sink（単一 node copy/cut・page 化 H1）を素通し + 完了条件が宣言した TC レンジ（12..20）を非連続サブセットで実装し欠番（14/15/20）を別レンジ（01..19 green）報告で隠蔽
- 現象: Shift+Enter で node.text に `\n` が入るようになった sprint で FR-SE-03「plain text は 1 node = 1 行」を実装。だが `node.text` を外部向け文字列に流す sink が複数あるのに **1 つ（複数選択の `getSelectedText()` :4150）だけ**に `replace(/\n/g,' ')` を適用し、兄弟 sink 3 種を素通しにした: (a) 単一 node の Cmd+C（outliner.js:5308 `singleText`）/ Cmd+X（:5352 `cutSingleText`）が raw node.text を OS クリップボード text/plain に流す（外部 paste で行分裂）、(b) `makePage`（:5768）が multi-line node.text を `host.makePage` 第3引数に流し作成 page md の H1（`# ${title}\n`）に継続行が漏れる、(c) `handleNodeTextLineBreak`（:5555）が `syncPageH1FromNodeText` を呼ばず、行途中 Shift+Enter 後に入力せず blur で H1 stale。並行して、TASK-02 完了条件が「**TC-SE-12..20 green**」と連続レンジを宣言したのに generator は TC-SE-12/13/16/17/18/19 の**非連続サブセット**だけ実装し、14/15/20 を欠落。generator-log は「TC-SE-01..19（19/19）green」と**宣言と別のレンジ**で報告したため、宣言レンジの端（14/15/20）が落ちた事実が report 上見えなかった。フルスイート 2786 green・tsc clean だったのは、欠落 TC が存在しない（=踏めない）+ 単一 copy/page 化 H1 を踏む番人が 1 本も無かったため
- 原因: (1) FR「plain text は 1 行」を「複数選択 copy の潰し」で代表させ、**同じ node.text を外部文字列化する全 sink を `grep` で数えなかった**（単一 node copy/cut は multi-select と別コードパス :5308/:5352、page 化は :5768、行内改行後 H1 sync は :5555）。designer/generator_failures 頻出「N 経路を代表数個で全部と誤認 / 共有判定を一部経路にだけ配線」の node.text-sink 版。(2) 完了条件のレンジ宣言（`TC-SE-12..20`）と実装した TC 集合を突き合わせず、report では別レンジ（`01..19`）を主張した。連続レンジ表記は「両端＋間の全番号」を含意するのに、非連続実装との差分を誰も diff しなかった（generator_failures 2026-08-02「targeted サブセットの green を フル green と報告」の TC レンジ版）
- 回避策: (a) **「model 値 X を 1 行化/正規化/サニタイズする」FR を実装したら、`grep -n "node.text\|\.text\b" <file>` で X を外部（クリップボード text/plain・md・H1・リンクラベル・llms）へ流す全 sink を列挙し、各 sink に変換を適用**（or 意図的に raw 保持する sink は理由をコメント）。単一 vs 複数選択のように「同じ操作の並列コードパス」は片方だけ直すと必ず非対称になる。今回の分離規則: **plain text（外部往復）は潰す / nodesData（内部・cross paste 往復）は raw 保持 / H1 系（node→md）は先頭行のみ** を全 sink で統一。(b) **完了条件が TC レンジ（`TC-SE-12..20`）を宣言したら、実装後に `grep -oE "TC-SE-[0-9]+[a-z]?" <spec>` で実在 TC 集合を出し、宣言レンジの全番号が揃うか diff する**。非連続サブセットで実装したら report のレンジ表記を実集合に合わせる（`01..19` のような連続表記で欠番を隠さない）。reviewer は「宣言レンジ ∖ 実装 TC 集合」を必ず計算して欠落 pin を検出する（今回 {14,15,20} が欠落）。(c) node.text-sink / clipboard 系の番人 TC は「複数選択」だけでなく **単一 node copy/cut（selectedNodeIds.size===0・selection collapsed 経路）** と **page 化（@page/Cmd+Enter で multi-line node → 作成 md の H1）** を各 1 本踏む（代表 1 経路の green で「全 sink OK」としない）

## 2026-07-26 [20260726-013730-clipper-skills-flat-layout] 正典の多分岐 fallback をミラー実装するとき、片方のミラー（skills）は全 branch を転記したのに他方（clipper）で最後の 1 branch を脱落 + その branch を踏まない TC で番人不在
- 現象: 本体 `src/shared/flat-layout.ts:73-82 resolvePagesDir` は hint 無し・hasFlatMd=false 時に **2 つの legacy branch を順に**見る（①`<base>/<stem>/` ②`<base>/pages/` → 無ければ flat）。この解決順を 2 サブシステムにミラー実装する sprint で、skills 側（fractal-md.mjs:55-58 / fractal-search.mjs:32-35）は **両 branch を字面 1:1 転記**したのに、clipper 側 `flat-layout-mirror.js:58 chooseWriteDirRel` は `hasLegacyMd`（= `<outId>/` = stem branch のみ）しか受けず **`pages/` branch を完全に脱落**（`grep -c pages flat-layout-mirror.js` = 0）。→ pages-legacy note（`<note>/pages/*.md` + pageDir 未指定 .out。memory「flat migration legacy layouts」の通り pages/ は pageDir 未指定 .out の残骸として実データに分散）に clip すると、本体は pages/ を読むのに clipper は flat（root）に書き、書いた page md が root に落ちて hasFlatMd を反転させ **pages/ の既存 page md ごと本体から見えなくなる**（design-review が HIGH② で指摘した「本体が読めない場所に書く」データ整合失敗の pages 変種。stem branch は HIGH② 修正で直したが pages branch は未修正で残った）。番人 TC-CF-04 は stem-legacy（hasLegacyMd）ケースしか踏まず、pages ケースを 1 件も持たないため 15/15 green ですり抜けた
- 原因: (1) 多分岐 fallback（本体 resolvePagesDir の legacyByStem + legacyPages）を「1 個の hasLegacyMd フラグ」に潰し、正典の branch 数を数えずに済ませた。skills ミラーが 2 branch を持つのに clipper が 1 branch という**同一 sprint 内のミラー非対称**を突き合わせなかった。(2) 番人 TC が「代表 1 branch（stem）」だけで「legacy 全部」を検証したつもりになり、正典の各 branch を 1:1 で踏まなかった。designer/generator_failures 頻出の「N 経路ある処理を代表数個で全部と誤認」と同一クラス
- 回避策: (a) **正典の多分岐 fallback をミラーするときは、正典の branch を機械的に数え（`grep -n "return .*legacy\|if (.*existsDir" flat-layout.ts`）、全 branch を 1:1 転記する**。「hasFlatMd → legacy → flat」の legacy が実は 2 種（stem + pages）ある点を潰さない。(b) **同一 sprint で複数サブシステムに同じ解決順をミラーするなら、ミラー同士を突き合わせて branch 数が一致するか確認**（skills は 2 branch なのに clipper は 1 branch = 片方が脱落のサイン）。(c) 番人 TC は正典の**各 branch を 1 件ずつ**踏む（stem-legacy / pages-legacy / flat / hint を全て）。counterfactual は「その branch を削ると本体が読めない場所に書く = FAIL」を各 branch で実測。「代表 1 branch が緑」で「legacy 全部 OK」としない

## 2026-07-26 [20260726-013730-clipper-skills-flat-layout] 解決ヘルパを export + unit 検証したが実書き込み経路から呼ばず dead code 化（FR「ヒント尊重」未達 + 非 load-bearing TC）
- 現象: FR-CL-02「imageDir ヒント（.out）があれば尊重」に対し、`flat-layout-mirror.js:73 resolveImagesDirRel`（imageDir ヒント尊重・default `images`）を export + TC-CF-05 で検証したが、**実書き込み経路（popup.js:299 / background.js:306）から一度も呼ばれない**。画像実体化は `DataUrlImageExtractor.processDataUrlsInMd(md, pageDirHandle)` が `pageDirHandle/images/` を**ハードコード**（data-url-image-extractor.js:114）するため、非デフォルト imageDir ヒント（例 `./assets/img`）を持つ .out で画像が `images/` に落ち本体の解決先とズレる。本体 resolveImagesDir（resolveSharedSub）と skills resolveOutlinerAssetDir（explicitDir 分岐）は共にヒントを尊重するのに clipper だけ未尊重。TC-CF-05 は「実挙動を守らないヘルパ」を検証しており、実書き込みが imageDir を無視しても緑のまま（非 load-bearing）
- 原因: 解決ヘルパを「書いて export して unit で緑」までで完了とし、それを**実際に呼ぶ配線**（extractor に渡す）を繋がなかった。TC がヘルパ単体を検証するだけで「ヘルパの結果が実経路に流れる」ことを確認しなかったため、dead code が緑で通った
- 回避策: (a) 解決/変換ヘルパを新設したら、`grep -rn "<helper>" <実経路ファイル>` で**実書き込み/実行経路から呼ばれているか**を確認する（export + unit 緑だけで完了としない）。呼ばれていなければ dead code = FR 未達のサイン。(b) FR が「X を尊重する」を要求するなら、X が実際に効く配線（ヒント → 解決 → 実 I/O）を 1 本通してから、その配線を番人化する。ヘルパ単体 unit は「配線が繋がっていれば」正しい値を返すことしか保証しない（配線断裂を検出できない）。(c) ハードコード値（`getDirectoryHandle('images')` 等）が解決ヘルパの結果で置き換わっているか、実 I/O 直前の値を確認する

## 2026-07-28 [20260728-135515-list-cut-paste-structure] 「Backspace と同型」を謳った cut 掃除の昇格ループに textContent 条件を独自追加し、img/checkbox のみの nested 子 li を silent drop（asset 1:1 ownership 違反の新規データ損失）
- 現象: リスト cut の空 li 殻残りを直すため「Backspace 範囲選択（editor.js:7622-7693）と同型の掃除」を cut ハンドラに追加したが、(i) affected-li の hasContent 判定（editor.js:17727-17733）が `<img>` を `textContent.trim()===''` ゆえ content 扱いせず `<input>`(checkbox) を `continue` で skip → **img/checkbox のみの li を空殻とみなし `li.remove()`**、(ii) 昇格ループ（editor.js:17740-17747）に **`if (nl.firstChild.textContent && trim())` の条件付き昇格**を独自追加 → img-only / checkbox-only の nested 子 li が textContent 空のため昇格されず `nl.removeChild` で破棄。reviewer が real Chromium（standalone-editor.html・in-memory）で 2 ベクトル実証: (A) `bbb<img>` の bbb だけ cut → img ごと li 消滅、(B) `parenttext` + 子 `[img][keepchild]` で parenttext cut → keepchild は昇格生存するが img は消滅（checkbox も同様）。新規 TC-CX-01〜06 全 green + unit 509 green だったのは、TC 群が img/checkbox を 1 件も踏まず（select-all 単一要素 or text-only fixture）full-green ですり抜けたため
- 原因: (1) 「Backspace baseline と同型」と宣言しながら、Backspace の昇格ループ（editor.js:7679-7683）が**無条件昇格**（`while(nl.firstChild) parentList.insertBefore(...)`）で img/checkbox 子を落とさないのに対し、cut は空殻残り回避のため昇格に textContent 条件を足して baseline から乖離させた（「同型」が昇格ループで破れた）。(2) 既存コード自身が「img has empty textContent but is real content」（editor.js:8604-8606 / 7799-7801）と明記する img=content 規約を、削除/昇格の空判定で参照しなかった。(3) 番人 TC が text-only リストしか踏まず、fractal で常態の「画像を bullet に持つ」形を 1 件も検証しなかった。MEMORY「asset 1:1 ownership invariant」（node/md と 添付・画像は 1:1 所有、move/copy/migrate 全経路で保持）に真っ向違反で、旧 cut（deleteContents のみ）が保持していた img を落とす回帰
- 回避策: (a) DOM 要素を「空だから削除/破棄」する判定を書くときは、**img/checkbox/その他 void 要素（textContent が空でも content）を content として認識する**。既存コードに `img has empty textContent but is real content` 型の規約（editor.js:8604）があれば必ず踏襲する。textContent.trim() だけで「空」を判定しない。(b) 「既存 X（Backspace 掃除）と同型」を謳うなら、昇格/削除の**各ループを baseline と字面で突き合わせ**、独自に足した条件（textContent 昇格ガード等）が baseline の不変条件（無条件昇格 = 子を落とさない）を壊していないか確認する。空殻残り回避と資産保持は両立させる（真に空 = text も img も input も nested list も無い、な殻のみ破棄）。(c) リスト/ノードの削除・移動・掃除の番人 TC は **img/checkbox/添付を含む bullet を必ず 1 件踏む**（fractal は画像を bullet に持つのが常態）。text-only fixture だけの full-green は asset 損失を検出できない。reviewer は asset を扱う DOM 掃除/移動の変更で「img/checkbox を含む要素が生存するか」を real ブラウザ（in-memory・disk 非変更）で独立実測してから PASSED を出す

## 2026-07-29 [20260729-000358-ol-start-preserve] merge 抑止判定を mergeAdjacentLists 経路にだけ入れ、同じ「隣接同型リストへ吸収する」convertToList('ol') の独自 raw tagName merge を素通しにして start≠1 ol を無言破壊（設計の明示指示を silent skip）
- 現象: ol 開始番号保持 sprint で、隣接 ol の start 無言消滅を防ぐ `areListsCompatible`（実効 start 連続性判定・editor.js:12488）を新設し mergeAdjacentLists（:12511）に配線したが、**同じく「隣接する同型リストへ吸収する」もう 1 つの経路 `convertToList(type)`（:15021-15066）の merge 2 分岐（:15034 next へ prepend / :15047 prev へ append）を raw `sibling.tagName.toLowerCase() === type` のまま**にした。到達経路は Ctrl+Shift+O（editor.js:14690）→ `if (!convertListToType('ol')) convertToList('ol')` で、cursor が段落（リスト外・convertListToType が :15187/:15190 で false）のとき convertToList('ol') が走る。隣接に `<ol start="5">` があると無条件 prepend され、既存項目の表示番号が 5→6 に無言シフト（新段落が「5」の枠を奪う）＝ NFR-OLS-01/03（既存挙動非破壊・属性生存）違反。design/system.md §1⑤ が「**convertToList の隣接 merge が独自判定なら、同じ実効 start 連続性チェックを足す**（generator は :14962-14984 を確認して対称にする）」と明示していたのに実装が未消化・generator-log に判断記録なし（silent scope reduction）。新規 spec 19/19 green・NEW-RED 0 だったのは、番人 TC が mergeAdjacentLists 経路（TC-OL-13）と convertListToType('ol') 経路（TC-OL-16・cursor がリスト内）のみで、convertToList('ol')（段落→ol）の隣接 merge を 1 件も踏まなかったため
- 原因: 「隣接リストへの吸収」を行う経路が複数（mergeAdjacentLists / changeParentListtype→mergeAdjacentLists / convertToList / convertToTaskList）あるのに、新判定を「areListsCompatible を呼ぶ経路」にだけ入れ、独自 raw tagName で merge する convertToList を対称化しなかった。設計が明示的に「convertToList adjacent も同ロジックに揃える」と指示していたのに、そのファイル位置（旧 :14962、現 :15034/:15047）を確認せず落とした。designer/generator_failures 頻出の「N 経路ある処理を代表数個で全部と誤認 / 共有判定を一部経路にだけ配線」と同一クラス（今回は既存データ = 隣接 ol の start を破壊する回帰）
- 回避策: (a) 「不変条件を守る判定（areListsCompatible 等の merge 可否）」を新設したら、その不変条件が関わる**全ての『隣接同型要素へ吸収する』経路**を `grep -n "nextElementSibling\|previousElementSibling\|tagName.*=== *type\|tagName.toLowerCase() ===" <対象関数群>` で洗い出し、raw tagName 判定で merge している経路（convertToList / convertToTaskList 等）を全て新判定に通す（「関数自身にガード」の思想を、共有判定を呼ばない独自 merge 経路にも適用）。「areListsCompatible を呼ぶ経路だけ直した」で締めない。(b) 設計が「X も同ロジックに揃える（generator は :NNNN を確認して対称にする）」と明示指示した箇所は、実装前にその行を Read して現在位置を特定し、消化 or 明示 defer（generator-log に理由記録）する。無言で落とさない。(c) merge 抑止/連続性の番人 TC は、mergeAdjacentLists 経路だけでなく **convertToList('ol')（段落→ol・cursor がリスト外）経路**も 1 件踏む。到達経路が「convertListToType が false のとき convertToList」のような分岐フォールバックの場合、フォールバック側（段落上での操作）を明示的に setup して踏む。reviewer は「隣接リスト merge の start 保護」sprint で、start≠1 ol の隣に段落を置いて convertToList('ol') を駆動する counterfactual を real ブラウザで独立実測してから PASSED を出す

## 2026-08-01 [20260801-200307-export-multinode-dnd-anyext] defense-in-depth（sanitize）を local shadow 関数に置き、同名の shared 版（Notes md 面が実際に呼ぶ実装）を見落として FR の防御が別 provider に届かず + global `..` replace が正当な連続ドット名を破壊
- 現象: FR-DDX-02（md host の添付ファイル名 sanitize = basename 化 + traversal `..` 防御）を、`src/editorProvider.ts:138` の **local `function generateUniqueFileNamePreserving`（module-private の shadow）**にだけ追加した。だが同名関数は `src/shared/paste-asset-handler.ts:803` にも **export で存在**し、こちらは sanitize なし。**Notes 内 md エディタの D&D 保存（notesEditorProvider.ts:34 が shared 版を import → saveMdFileToDir:1129 / readAndInsertMdFile:1152 / saveMdImageToDir:1081 / readAndInsertMdImage:1106 が shared 版を呼ぶ）**は防御が一切効かない。requirement は「md 側 host」と書いており Notes md host も含意するので、FR-DDX-02 の防御が md editor 面（editorProvider）にしか届かず Notes md 面が無防備なまま。加えて追加した sanitize 自体が `path.basename(...).replace(/\.\./g, '')` の **global replace** で、`archive..tar.gz`→`archivetar.gz`・`v1..2.pdf`→`v12.pdf` と正当な連続ドット名を破壊する（basename 後に危険なのは厳密名 `.`/`..` のみなのに over-broad）。全 sprint TC green・tsc clean だったのは、番人 TC-DDX-06 が **source-contract（editorProvider.ts source に `path.basename`/`replace(/\.\./g,'')` 文字列が在るか）だけを assert** し、(1) shared 版に防御があるか、(2) 正当な連続ドット名が保持されるか、を 1 件も踏まなかったため（tautology 気味の存在 assert）
- 原因: (1) 「ガードは共有関数自身に置く」教訓（designer_failures 2026-07-25「限定発火を共有シングルトンの呼び出し側分岐で担保し関数自身にガードを置かず」と同型）を、同名関数が **local shadow + shared export の 2 実装**で存在する状況で適用し損ねた。設計 §1③ は「editorProvider.ts:135 付近の generateUniqueFileNamePreserving 入口で」と local 版の行だけを指しており、generator が `grep -rn "generateUniqueFileNamePreserving" src/` で全定義（shared export + local shadow）と全 caller（notes 4 経路含む）を数えず、設計が指した 1 定義だけ直した。(2) sanitize の correctness を「`..` を含めば消す」の blacklist 発想で書き、`path.basename()` が既に traversal を無害化済み（返り値に `/` は無く危険は厳密名 `..`/`.` のみ）という事実から許可範囲を最小化しなかった（designer_failures 2026-07-24「サニタイズを blacklist で書き closed allowlist にしない」と同型）
- 回避策: (a) **防御/sanitize を関数に足すときは `grep -rn "<funcName>" src/` で同名の全定義（export 版 / 各ファイルの local shadow）を数え、実際に該当経路（今回 Notes md host = shared 版）が呼ぶ実装に防御を置く**。「設計が指した 1 行の関数」だけ直さない。同名 2 実装があれば防御は**共有 export 版に置く**（全 caller が一律防御される + shadow の footgun 解消）。(b) **basename 後の sanitize は厳密名ガード**（`if (name === '.' || name === '..') name = 'file'`）にする。global `/\.\./g` replace は正当な連続ドット名（`archive..tar.gz`）を破壊するので使わない。`path.basename()` はディレクトリ成分を除去済みで、残る危険は厳密名 `.`/`..` のみ（closed allowlist 発想）。(c) **sanitize/防御の番人 TC は source-contract（`toContain('path.basename')` 等の文字列存在 assert）で締めず behavioral にする**: 実関数を require（shared 版は vscode 非依存で require 可・`test/unit/flat-pathbuilder.spec.ts` が先例）し、正当な連続ドット名の保持 + 厳密名 `..` の dir 外書き込み不可 + counterfactual（global replace に戻すと正当名破壊で RED）を踏む。「source に防御文字列が在る」だけでは Notes 面に届いているか・正当名を壊さないかを検出できない（tautology 番人）。reviewer は「sanitize/防御を足す」sprint で、防御対象の FR が指す**全 provider/host 経路**（editor + notes）が実際に防御を通る実装を呼ぶか、`grep -rn` の全 caller で独立確認してから PASSED を出す

## 2026-08-02 [20260802-212934-aws-sdk-migration] esbuild 移行で `tsc -p ./`（全 src emit）を `--noEmit` にした結果 out/shared の TS 由来 .js が消え、それを module-scope require する既存 spec が Playwright の collection を全中断 → `Total: 0 tests in 0 files` の false-green（generator は限定 targeted で「604 green」報告し 9 fail を見逃した）
- 現象: compile を `tsc -p ./`（extension も shared も全 src を out に個別 emit）→ `tsc -p ./ --noEmit && node esbuild.js`（型チェックのみ + extension 単一バンドル）に変えた。design §4「out/ 一本化 = stale 二重構造の排除」は extension エントリグラフの話だったが、**副作用として TS 由来の `out/shared/*.js`（paste-asset-handler / drop-import / outliner-clipboard-store / md-h1-utils + それらの TS transitive dep）が生成されなくなった**。copy-webview.js は手書き `src/shared/*.js`（15 本）しかコピーせず TS 由来を出さない。→ これらを **モジュールスコープ**で require/import する 3 spec（unit-drop-import.spec.ts:13 top-level import / outliner-export-bundle.spec.ts:17 / outliner-node-paste-assets.spec.ts:16 top-level require）が collection 段階で throw。Playwright はモジュールスコープの require throw で**全 collection を中断**するため `npx playwright test --list` = **`Total: 0 tests in 0 files`**（errors 3 件）、`npx playwright test` は **0 件実行のまま "成功"（false green）**。in-body require の 5 fail（unit-paste-asset-unified ×2 / md-dnd-anyext TC-DDX-07 / skills-search-filters ×6）も含め計 9 fail が隠蔽された。**known-red gate（check-known-red.sh）も欺かれた**: JSON の `suites` を walk して `unexpected` を数えるだけで top-level `errors[]` を見ないため、collection abort 時 current=∅ → `new_fails=∅`(exit 0) かつ `fixed=baseline 全件` → `NEW FAILS 0` + `FIXED 100` の偽出力（baseline はちょうど 100 件で符合）。generator-log は「フル unit 604/604 green」と限定 targeted サブセットで報告し、この回帰に一切言及しなかった（generator_failures 2026-07-11/24 の「targeted で全 green を主張し実測と食い違う」と同型 + collection abort による更に強い false-green）
- 原因: (1) ビルド構成を「per-file emit → バンドル」に変えるとき、**旧 `tsc -p ./` が test-facing 成果物として出していた `out/shared/*.js`**（extension エントリではなくテストが require する副産物）を保存する責務を落とした。「out/ 一本化」を extension バンドルの話としてだけ捉え、テストが依存する out 成果物の存在を確認しなかった。(2) 「フル unit green」の根拠を、collection abort する spec を含む正しい runner（`npx playwright test` / run-parallel-tests.sh）でなく限定 targeted サブセットで主張した。abort する 3 spec が targeted に含まれていれば即座に露見した。(3) known-red gate が「0 件実行」を「fail 0」と誤認する穴（top-level errors[] 非考慮）を突かれた
- 回避策: (a) **ビルドの emit 範囲を変える変更（tsc → esbuild / --noEmit 化 / outDir 変更）では、`grep -rhoE "out/[a-z/-]+" test/` でテストが require する全 out 成果物を列挙し、変更後もそれらが生成されるか確認する**。extension バンドル単一化の意図は、shared を別 tsconfig（`tsconfig.shared-emit.json` で `src/shared/**` を out/shared に scoped emit）で出せば完全に両立する。テストが require する out ファイルを消す変更は NFR「既存テスト非破壊」違反。(b) **`npm run compile` 後に必ず正規の runner（`npx playwright test` or `--list`）で collection が成功するか確認してから「N green」報告する**。`Total: 0 tests in 0 files` は最大の red flag。targeted サブセット（`npx playwright test <一部 spec>`）の green を「フル green」と報告しない。(c) **module-scope で out 成果物を require/import する spec は 1 本でも解決失敗すると全 collection を道連れにする**ことを認識する（in-body require なら個別 fail で済む）。(d) reviewer は「N green」報告を鵜呑みにせず自ら `npx playwright test --list` で collection 成功を確認し、known-red gate は JSON の `errors[]` 長 > 0 or 収集 spec 数が閾値未満なら `exit 2`（COLLECTION FAILED）を返すよう改修して false-green を構造的に塞ぐ

## 2026-08-02 [20260801-232943-outliner-dnd-highlight-reliability] 症状 A 掃除用に新設した window 安全網が highlight だけ消し dropIndicator を消さず、掃除経路を片肺化（解除経路の片系統漏れ）
- 現象: outliner D&D の drop highlight 残留（症状 A）修正で、drag セッションの終わり方（shift なし D&D・webview 外 drop）を掃除する window `dragend`/`drop`(capture) 安全網（outliner.js:956-960）を新設したが、`clearDropZoneHighlight()`（treeEl の水色枠 class を消す）**のみ**を呼び `removeDropIndicator()` を呼ばなかった。他の全解除経路（treeEl capture drop :977-978 / treeEl drop :1020-1021,1028-1029 / node drop :3378,3390）は 2 つを並べて呼ぶのに、この新設 1 経路だけ非対称。→ symptom-A の shift なし D&D を **node 上で**行うと node dragover（:3326-3337 showDropIndicator）が dropIndicator（child position は 1px dashed 箱 :1404-1406）を生成 → drop 不発で window dragend 終了時に安全網が dropIndicator を消さず DOM に残留（本 sprint が症状 A 掃除のために新設した経路自身が、ユーザー報告の「破線枠残留」を掃除し損ねる片肺）。pointer-events:none ゆえ後続ブロックはしないが視覚アーティファクトは残る。番人 TC-DH-01 が「highlight class 消滅」のみ assert し dropIndicator 残留を見逃した（tautology 気味の穴）ため 6/6 green ですり抜けた
- 原因: TASK-01 が「解除を 1 ヘルパ clearDropZoneHighlight に集約し全経路から呼ぶ（共有判定を一部経路にだけ配線の失敗クラス回避）」と明記していたのに、集約対象を **highlight class のみ**とし、drop 視覚要素がもう 1 つ（dropIndicator）ある事実を見落とした。「掃除すべき視覚状態」を highlight class 単体で数え、node dragover が別途生成する dropIndicator を勘定に入れなかった。designer/generator_failures 頻出「N 系統ある解除/掃除を代表 1 系統で全部と誤認 / 共有判定を一部経路にだけ配線」（2026-07-21/07-29 等）と同一クラス
- 回避策: (a) 「drag/hover 中の視覚状態を全経路で掃除する」修正では、その操作が生成する**全ての視覚要素/状態**を数える（今回 = treeEl の highlight class + tree に append される dropIndicator の 2 系統）。片方だけ消す掃除経路を残さない。掃除ヘルパを作るなら「highlight + dropIndicator を両方消す 1 ラッパ」に集約し、新設の安全網も既存経路も同じラッパを通す。(b) 掃除の番人 TC は「class が消えた」だけでなく **生成された全視覚要素（`.outliner-drop-indicator` の DOM 不在含む）が消えた**ことを assert する。dragover 後に dropIndicator が存在することを先に確認してから（= 掃除対象が実在する setup）、セッション終了後に両方消えることを counterfactual（掃除の片方を戻すと残留 RED）で固定する。「highlight class だけ見る」assert は片系統漏れを検出できない

## 2026-08-02 [20260802-075012-md-pdf-export] 権威 getter の可視性ガードを DOM フォールバックが再現せず、隠れた stale md を PDF 対象に採用（設計に無い追加フォールバックが不変条件を落とす）+ cancel リトライの one-shot ガード漏れ
- 現象: (A) `resolvePdfTarget`（pdf-export-webview.js:142-172）に**設計 §2.1 に無い DOM フォールバック**（:149-152 `_queryMainEditor()` 採用 / :159-162 `.side-panel .editor`）を追加したが、権威 getter（notes-md-dispatcher.js:132-137 `mainMd.getEditorEl`）が持つ `markdownContainer.style.display === 'none'` の可視性ガードを再現しなかった。機序: Notes dispatcher の md→.out タブ切替は `showOutliner()`（:51）で `display:none` にするだけで innerHTML をクリアしない → 隠れた stale md `.editor` が残留。DOM フォールバックの `_isOutlinerMode()`（:188-200）は「main .editor が空」を outliner 判定条件にするため、stale md（`_hasContent` true）で false を返し `_queryMainEditor()` が隠れた stale を採用 → 「.out のみ・sidepanel 閉」で前に見ていた md の PDF が無言生成 = **FR-PDF-01 受け入れ 4 行目（対象なし・副作用ゼロ）違反**。`notesEditorProvider.getActivePanelForPdf`（:64-71）は panel.active のみ見てタブ種別を見ないので到達可能。番人 TC-PDF-43 は「空 .editor → null」だけを踏み、この「隠し stale md 残留」状態を 1 件も踏まないため 74/74 green ですり抜けた（tautology 気味の穴）。(B) `pdf-export-host.ts:260-282` の cancel 配線は `onCancellationRequested`（one-shot）を第 1 execFile の kill に使うのみで、`result.code!==0` の legacyHeadless リトライ（:271-279）前に `token.isCancellationRequested` を確認しない → cancel 後もリトライが第 2 Chromium を最大 120s 暴発（cancel が二度と効かない）。
- 原因: (A) 「webview が自己解決する（真実は webview が持つ）」設計に対し、権威 getter（closure の mdInstance / display ガード付き）だけで足りるのに DOM フォールバックを保険で足し、その保険が**権威側の不変条件（可視性 = display!=='none'）を落とした**。「stale を掴まないための display ガード」は権威 getter にはあるが、後から足した DOM 経路には無い＝同じ不変条件が全経路に配線されていない（designer/generator_failures 頻出「共有判定を一部経路にだけ配線」の可視性ガード版）。`_hasContent`（中身の有無）と可視性（display）は直交する軸なのに、`_hasContent` だけで採用可否を決めた。(B) one-shot state（`onCancellationRequested` は 1 度発火したら再発火しない）を使う経路で、リトライという**第 2 の発火機会**に対し `isCancellationRequested` の再チェックを置かなかった（set 契機だけ見て消費/再確認契機を落とす one-shot ライフサイクルの穴）。
- 回避策: (a) 「権威 getter（closure/state ベース）+ DOM フォールバック」の二段構えで対象を解決する設計では、**権威側が持つ全ガード（可視性・stale 回避・close 後 null 化）を DOM フォールバックにも同じく配線**する。特に「display:none だが DOM は残留」は Fractal の頻出パターン（タブ切替が innerHTML をクリアせず隠すだけ = notes-md-dispatcher.js:51/showOutliner）なので、`.editor` を掴む全経路で `offsetParent===null` or 祖先 `display:none` を弾く。`_hasContent`（中身）と可視性は別軸として両方見る。(b) 対象解決/フォールバックの番人 TC は「空 → null」だけでなく **「隠れた stale 内容が残留 → null」**（display:none 配下に中身のある `.editor` を置く）を counterfactual（可視性ガードを外すと stale 採用で RED）で踏む。(c) `onCancellationRequested`（one-shot）で child を kill する経路にリトライ/再 spawn がある場合、リトライ前に必ず `if (token.isCancellationRequested) return;` を置く。番人 TC は「第 1 実行中に cancel → 第 2 実行が呼ばれない（execFileCalls===1）」を counterfactual（ガードを外すと ===2）で固定。one-shot state の消費経路は「発火後に再度到達しうる分岐」を全て洗い、そこで `isXxxRequested` を再確認する。

## 2026-08-05 [20260804-145603-fractal-note-polish-batch] URL スキームの id セグメントを decodeURIComponent 後に containment チェックなしで path.join に流し path traversal（既存の safeResolveUnderDir を同 sink に適用せず）
- 現象: FR-B11 で新設した md link `fractal://note/{folder}/md/{mdFileId}` の解決経路（notesEditorProvider.ts:2619 navigateToLink → getMdFilePath → flat-layout.ts:177 resolveMdFilePath）が `path.join(mainFolder, `${id}.md`)` を containment チェック無しで実行。入口 inapp-link-utils.js:53 の正規表現 `fractal://note/([^/]+)/md/([^/?]+)$` は `..%2F..%2F...`（リテラル `/` を含まない）にマッチし、:57 `decodeURIComponent` が**マッチ後に** `%2F` を `/` に復号 → raw で path.join に流入。`fractal://note/{folder}/md/..%2F..%2F..%2Fetc%2Fpasswd_copy` で note フォルダ外の .md を開け、openFile が currentFilePath を設定するため編集保存で書き戻しも成立。folder セグメントは等値検索（extension.ts:359）で安全。コードベースには正しいヘルパ `safeResolveUnderDir`（src/shared/path-safety.ts。cleanup-core.ts / notesEditorProvider.ts の node.filePath 解決で使用中・**同ファイル line 18 で既に import 済み**）があるのにこの sink に未適用。新規 TC-B11-01..07 は全 green だったが traversal 入力（encode 済み `..%2F`）を 1 件も踏まないためすり抜けた
- 原因: (1) URL/スキームの動的セグメント（id/name）を「生成 id だから安全」と暗黙前提し、それが外部入力（ユーザーが手で貼れる fractal:// リンク）でもある事実を軽視した。(2) `decodeURIComponent` が正規表現マッチ**後**に走るため、`[^/?]+` の「/ を含まない」制約は復号前の `%2F` を素通しする（復号後は `/` を含む）。エンコード境界を跨ぐと文字クラス制約が traversal 防御にならない。(3) 既存の path 解決 sink（node.filePath）は safeResolveUnderDir で clamp 済みなのに、md 面へ機能拡張する際に同じ clamp を新 sink に配線しなかった（designer/generator_failures 頻出「共有判定/防御を一部経路にだけ配線」の path-safety 版）
- 回避策: (a) **URL/スキームの動的セグメントを path.join に流す前に必ず `safeResolveUnderDir(baseDir, decodedSegment)` で clamp**する（null なら中断）。「生成 id だから」を防御を省く根拠にしない（fractal:// は手で貼れる外部入力）。(b) 正規表現の文字クラス（`[^/]+` 等）は **decode 前の文字列**にしか効かない。`decodeURIComponent` を挟むなら、復号後の値に対して containment を再検証する（復号で `/` が生じうる = マッチ時の「/ を含まない」保証は失われる）。(c) 既存機能に path 解決の clamp（safeResolveUnderDir 等）があるなら、その機能を別面（md / node / out 各 link）へ拡張する際は**新 sink 全てに同じ clamp を配線**する。理想は解決関数自身（resolveMdFilePath）に clamp を入れ全 caller を一律防御（getMdFilePath は notes-file-manager.ts で多用）。(d) 番人 TC は encode 済み traversal 入力（`..%2F..%2Fetc%2Fpasswd`）を渡し、解決結果が base 配下（or null）に収まることを counterfactual（clamp を外すと base 外を返す = RED）で固定。素直な id だけの TC では traversal を検出できない

## 2026-08-10 [20260809-031217-notetree-file-dnd] contenteditable 内アンカーの D&D を合成 DragEvent の TC で検証し「drag できる」と誤認 — real mouse では text selection が mousedown を奪い dragstart 不発（false-green）+ 同型 before バグの 3 ブロック中 2 つだけ修正
- 現象: (A) md editor 内 📎 file アンカーの tree D&D（FR-TF-15）で、TASK-17 が data-is-file-attachment + draggable=true をレンダ時付与し、TC-MX-06（`dispatchEvent(new DragEvent('dragstart'))` の合成イベント）が green だったため完了とした。だが手動テストで**drag 自体が全く開始できない**: contenteditable=true 内のアンカーは draggable=true でも **text selection が mousedown を奪い、ブラウザは element drag を開始しない**（real Chromium 実測。subpage アンカーが左端で drag できていたのは ::before アイコン領域がテキスト選択対象外だったからで、テキスト部分は同様に不発）。修正 = `contenteditable="false"` + CSS `user-select:none`（TASK-19）。(B) 同 sprint で「drop position='before' が先頭 target のとき末尾に化ける」バグ（「前兄弟 afterId を計算して addNode」— afterId=null が addNode の「null=末尾 append」に化ける）を TASK-18 で修正したが、同型ブロックが outliner.js に **3 つ**（dropFilesResult / importMdFilesResult / importFilesResult）あるうち 2 つだけ直し、importFilesResult に旧パターンが残存（reviewer iter3 QUAL-1。現状 host が after 固定送信で dead code のため実害なしだが同クラスの再発）
- 原因: (A) 合成 DragEvent の dispatch は「dragstart ハンドラが正しく setData するか」しか検証せず、**「ブラウザがそもそも drag を開始するか」**（mousedown からの OS ネイティブ D&D 開始 = contenteditable/user-select/draggable の相互作用）を踏まない。ユーザー操作の実現可能性を UI イベントの単体合成で代表した。(B)「同型バグを grep で全数列挙してから直す」を怠り、報告された 2 経路だけ直した（頻出「N 経路の一部にだけ配線」）
- 回避策: (a) **「ユーザーが drag できる」を検証する TC は real mouse 操作（page.mouse.down → move → up）で dragstart 発火を assert する**（合成 DragEvent dispatch はハンドラのロジック検証にしかならない — drag 開始可否は検証できない）。counterfactual = 対策（ce=false 等）を外すと dragstart 不発。(b) **contenteditable 内の要素を D&D 可能にするには draggable=true だけでは不十分**: `contenteditable="false"` + `user-select:none` を対で付与する（Fractal の確立機序 = TC-MX-08 先例。textContent の無い ::before アイコンだけが例外的に掴める）。(c) バグ修正時は同型パターンを `grep -n "<バグの字面（_getSiblingInfo + addNode 等）>" <file>` で全数列挙し、全ブロックに配線してから完了とする（dead code でも直す — 将来の到達で再燃する）

## 2026-08-09 [20260809-031217-notetree-file-dnd] webview 文言を i18n キー参照 + `||` 英語フォールバックで書き、WebviewMessages interface / 7 locale への登録を落として全 locale 英語固定（tsc も既存 TC も検出不能な silent i18n 債務）
- 現象: file item context menu の新設文言 3 件（notesOpen / notesRevealInFinder / notesAttachTooLarge）を notes-file-panel.js:494/498/901 で `i18n.notesOpen || 'Open'` 形式で参照したが、`src/i18n/messages.ts` の WebviewMessages interface にも 7 locale ファイル（en/ja/es/fr/ko/zh-cn/zh-tw）にも 3 キーを登録しなかった（grep 0 件）。同じメニューが使う既存キー（notesRename / notesDelete / copyPath / notesOpenInNewTab）は interface + 全 locale 登録済みで、新設分だけ非対称。`||` フォールバックがあるため実行時エラーも tsc エラーも出ず、全 locale で英語固定表示になる silent 債務として full-green をすり抜けた（webview 側 i18n オブジェクトは動的 record なので型チェックが効かない）
- 原因: webview の i18n は「キー参照 + フォールバック」で書けば動いてしまう（interface はホスト側の供給契約であって webview 側の参照を検査しない）ため、「動く = 完了」で locale 登録ステップを落とした。同一メニュー内の既存キーが登録済みという対称性も突き合わせなかった
- 回避策: (a) webview で新しい `i18n.<key>` 参照を追加したら、**同 commit 内で `grep -rn "<key>" src/i18n/` が interface + 7 locale 全部にヒットする**ことを確認してから完了とする（`||` フォールバックは「未登録でも落ちない」だけで i18n 完了ではない）。(b) reviewer は diff 内の新規 `i18n.\w+` 参照を `grep -oE "i18n\.[a-zA-Z]+" <diff>` で抽出し、各キーを src/i18n/ で逆引きして未登録キーを検出する（既存未登録キーとの切り分けは git blame / diff の +行判定で行う）

## 2026-08-10 [20260809-031217-notetree-file-dnd] 挙動改訂 TASK の実装で関数本体と直下コメントは新挙動に更新したが、同じ挙動を記述する関数直上 JSDoc / 分岐冒頭の項目列挙コメントを旧記述のまま残す（同一挙動を記述する複数コメントの片更新）
- 現象: (1) TASK-15（FR-TF-05b 改訂 = 子なし node は node ごと削除）で notes-message-handler.ts の treeFileRegisterFromOutNode 本体（:2306-2319）と本体直下コメントは新挙動に更新したが、関数直上の要約 JSDoc（:2266-2270）が旧挙動「元 node.filePath を null 化（所有移し替え）」のまま残った。(2) TASK-14（FR-TF-10 共通項目 3 件追加）で notes-file-panel.js の file 分岐に項目を実装したが、分岐冒頭の表示項目列挙コメント（:486-490）が追加前の 8 項目列挙のまま残った。どちらも実装・TC は正しく green で、コメントだけが改訂前の挙動を記述し次の読み手を誤誘導する
- 原因: 挙動を記述するコメントが 1 関数/1 分岐に**複数箇所**（直上 JSDoc / 冒頭の要約列挙 / 本体内インライン）あり、実装に最も近い 1 箇所だけ更新して残りを grep で洗わなかった。「コードと直近コメントが一致していれば完了」とし、同じ挙動を要約する上位コメントとの一致を確認しなかった
- 回避策: 挙動改訂 TASK（要件 rev / design_gap 裁定の反映）で関数の挙動を変えたら、**その関数・分岐に紐づく全コメント**（直上 JSDoc・分岐冒頭の項目列挙・インライン）を `grep -n -B5 "<funcName>"` で列挙し、旧挙動の記述（「null 化のみ」「N 項目のみ表示」等）が残っていないか確認してから完了とする。特に「表示項目/分岐の列挙コメント」は項目を追加/削除する TASK で必ず陳腐化するので、実装 diff と同 commit で同期する。reviewer は挙動改訂 TASK のレビューで対象関数の直上コメントと実装の一致を突き合わせる

## 2026-08-11 [20260810-183054-md-table-blocks-kiro-batch] li 内ブロックの隣接判定を「md ロード直後の綺麗な DOM」だけでテストし、実編集 DOM に混入する空白 text node で判定不達 — 手動テストで 3 rc 連続の再発(TC は全 green のまま)
- 現象: li 内 pre/blockquote の矢印進入・Backspace 統合を nextSibling/previousSibling の直読みで判定し、TC(setMarkdown でロードした DOM)は全 green。だが実機では「↓ でブロックを素通り」「bk が無反応」が再発。機序 = 実編集(タイプ・Shift+Enter・ブロック作成の繰り返し)で text/br/ブロックの間に空白 text node(''、' ')が挟まり、sibling 直読みが BR/PRE に届かない。rc.2→rc.3→rc.4 と 3 回の手動テストで同型の「判定不達」が別の顔で再発した
- 原因: fixture を setMarkdown(パーサが出す正規化 DOM)だけで作り、実編集が生む dirty DOM(空白 text node・分割された text node)を 1 件も踏まなかった。contenteditable の実 DOM は常に dirty である前提を欠いた
- 回避策: (a) li 直下の sibling 走査は必ず**空白 text node スキップ付き walker**(navSkipWs 型)を通す — 直読みの nextSibling/previousSibling を新規に書いたら赤信号。(b) DOM 隣接判定の番人 TC は「setMarkdown ロード形」と「空白 text node を意図的に挟んだ形」(li.insertBefore(document.createTextNode(''), ...) )の**両方**を踏む。(c) キー操作の統合 TC は可能な限り「実キー操作の連鎖」(タイプ → Shift+Enter → 作成 → 移動)で dirty DOM を自然に作ってから検証する

## 2026-08-12 [20260812-032645-md-table-select-merge-filter] 「合成イベント dispatch の TC」が発火可否を検証できず false-green — collapsed selection では copy イベント自体が不発なのに合成 ClipboardEvent で green
- 現象: table select モード(selection collapsed・キャレット無し)の cmd+c を、TC は `editor.dispatchEvent(new ClipboardEvent('copy'))` の合成イベントで検証し green。だが実機ではブラウザが「collapsed selection では copy イベントを発火しない」ため cmd+c が完全 no-op → OS クリップボードに残っていたスクリーンショットが cmd+v で貼られた(ユーザー報告)。ドラッグの合成 DragEvent(2026-08-10 記録)と同一クラスの 2 度目
- 原因: 合成イベント dispatch は「ハンドラに正しいイベントが来たときのロジック」しか検証しない。「ブラウザがそのイベントを発火するか」(collapsed selection × copy / contenteditable × dragstart 等の発火条件)は踏めない。イベント駆動の機能で発火条件が非自明な場合、合成 TC は構造的に false-green
- 回避策: (a) **クリップボード系の番人 TC は実キー(page.keyboard.press('Meta+c'))+ `context.grantPermissions(['clipboard-read'])` + `navigator.clipboard.read()` で「実際に書かれた内容」を assert する**。合成 ClipboardEvent はハンドラ単体の補助にのみ使う。(b) collapsed selection の UI(select モード・カスタム選択)で copy/cut を実装するときは copy イベント経由でなく **keydown 駆動 + navigator.clipboard.write** にする(イベント発火条件に依存しない)。paste はcollapsed でも発火するのでイベント経由で可。(c) 「合成イベントで green」の TC を書いたら「このイベントは実操作で本当に発火するか」(selection 状態・contenteditable・focus)を 1 度は実キー/実マウスで踏む

## 2026-08-13 [20260813-133248-search-doc-content] ZIP 自作パーサに伸長後サイズ上限を置かず zip bomb DoS の穴（圧縮後 50MB 上限だけで「サイズ対策済み」と誤認）+ 削除フローに派生データ（抽出キャッシュ）の evict を配線せず機密テキストが孤児残留
- 現象: (A) OOXML 抽出（doc-text-extract.ts + CLI ミラー）が `inflateRawSync(raw)` を maxOutputLength なしで呼び、central directory の uncompressedSize も検証せず。ファイル全体の 50MB 上限（圧縮後）はあるが、高圧縮率エントリ（deflate は 1000:1 超可）で 49MB の docx から GB 級 Buffer を確保させられる。TC は非 ZIP/stored/XML 破損のみで高圧縮エントリを 1 件も踏まず 41 TC full-green ですり抜けた。(B) deleteTreeFile が files/ 実体を trash に送るのに、検索が生成した抽出テキストキャッシュ（globalStorageUri 配下・本文全文のプレーンテキスト）を evict せず、「添付を消したら消える」期待の外で機密テキストが残留
- 原因: (A) サイズ防御を「入力ファイルサイズ」の 1 軸で数え、解凍という**増幅ポイントの出力サイズ**を別軸として数えなかった（decompression は入力検証でなく出力上限が本質）。(B) 削除フローの対象を「実体 + 台帳」で数え、機能追加（キャッシュ）が作った**派生データ**をライフサイクル対称の対象に入れなかった（作る経路だけ配線し消す経路を配線しない one-way 配線）
- 回避策: (a) **解凍・デコード・展開（inflate/unzip/base64/画像デコード）を呼ぶときは必ず出力サイズ上限を明示**（zlib 系は `{maxOutputLength}` + 宣言サイズ（uncompressedSize 等）の事前検証の二段構え）。「入力が N MB 以下だから安全」は増幅率を無視した誤認。番人 TC は高圧縮 fixture（ゼロ連続を deflate した合成 ZIP — spec 内生成で巨大 fixture を commit しない）を 1 本踏む。(b) **キャッシュ・インデックス・サムネイル等の派生データを新設したら、元データの削除・移動の全経路を grep で列挙して evict を対で配線**する（set 契機と clear 契機を対で設計する one-shot state の原則はキャッシュのライフサイクルにも適用）。reviewer は「派生データ新設 sprint」で削除経路の evict 配線を必ず確認する

## 2026-08-12 [20260812-110538-outliner-mindmap-batch] 回帰切り分けで working tree の stale ビルド成果物を「baseline の挙動」と誤認 — git stash/switch では build 生成物(standalone html)が追随せず、比較実験が汚染される
- 現象: gate 検出の notes-folder-indent fail を切り分けるため「git stash で変更除去 → pass / pop → fail」を観測し「自分の変更起因」と誤結論しかけた。実際は stash/switch 後に test:build:all を挟んだ/挟まないで standalone html の鮮度が変わっており、pass は「古い main ビルドの html」を見た誤測。クリーン worktree(git worktree add + main)での再実測により「main でも fail = 既存 red の顕在化(spec 期待が現行実装より古い)」と確定した
- 原因: テスト対象がビルド生成物(test/html/standalone-*.html)なのに、ブランチ操作(stash/switch)がソースだけを切り替え生成物は working tree に残ることを失念。生成物の鮮度がどのソース時点か追跡不能になった
- 回避策: (a) ビルド生成物を対象にした A/B 切り分けは **git worktree で独立チェックアウト + その場で rebuild** して比較する(stash/switch + 手動 rebuild の組合せは鮮度事故が起きやすい)。(b) 「baseline で pass」を主張する前に、その実行が読んだ生成物がどのソースから生成されたかを確認する(diff <(git show BRANCH:file) working-file)。(c) 既存 spec の期待値が古い可能性(実装が意図的に変わったのに spec が未更新・旧ビルドで潜伏)も切り分けの仮説に含める

## 2026-08-14 [20260813-210323-md-list-cut-nav-fixes] D&D の「掴みハンドル」を新規装飾（::before グリップ）で発明して全 file リンクにゴミ UI を付けた + 「所有移し替え」の元リンク除去を webview エコーだけに任せ fs-truth（removeMdAnchorFromFile）を新経路に配線せずデグレ
- 現象: (A) 📎 file リンクの「テキスト編集可 + drag 可」両立で、subpage の precedent（既存の見えているアイコン = 掴む場所）を確認せず 6-dot グリップ ::before を**新規発明** → 全 file リンクに見慣れないゴミ装飾が付いた（ユーザー激怒）。正解は既存グリフ（先頭 📎 絵文字）自体を ce=false span 化して掴みにする = 追加グリフゼロ。(B) 同 sprint で ce=false を撤去した際、「D&D 後に元リンクが残る」デグレが note md → tree / sidepanel md 経路で発生。機序 = treeFileRegisterFromMdLink / attachMdFileLinkToMd / linkMdSubpageToMd が removeFileLink/removeSubpageLink の **webview エコーのみ**で元 md を更新しており、source md の EditorInstance が同一 webview に生存している時しか効かない（2026-08-12 に importMd*IntoOut 経路で根治済みの既知機序 = removeMdAnchorFromFile による fs 単一真実化が、兄弟 3 経路に未配線のまま残っていた）。既存 TC は「removeFileLink message が飛ぶ」ことしか assert せず、fs 上の md からリンクが消えることを踏まなかった
- 原因: (A) 「掴む場所」という UI 装飾を、同型機能（subpage）の実際の見た目を確認せず新規に発明した。UI 追加は「既存の見えている要素を流用できないか」を先に問う。(B) 「X 経路で根治したパターン（fs-truth）」の兄弟経路（同じ removeFileLink エコーを使う全関数）を grep で列挙して配線しなかった — 頻出「共有防御を一部経路にだけ配線」クラス。message-echo の assert は fs の実状態を保証しない
- 回避策: (a) contenteditable 内で「テキスト編集可 + element drag 可」を両立するときの Fractal 確立パターン: **リンクの既存グリフ（絵文字/アイコン）を `<span contenteditable="false" class="*-grip">` で包み cursor:grab + user-select:none を当てる**（subpage は ::before、file は先頭 📎）。新規の視覚要素（::before グリップ・ドットハンドル等）を足さない。serialize が textContent ベースであることを確認（span は md に漏れない）。(b) 「所有移し替え」（D&D で元 md からリンクを外す）系の host 関数は **必ず removeMdAnchorFromFile（fs 直接除去）+ webview エコー（表示同期）の 2 段**にする。`grep -n "removeFileLink\|removeSubpageLink" notes-message-handler.ts` で全 postMessage サイトを列挙し、removeMdAnchorFromFile が対で呼ばれていない箇所は配線漏れ。番人 TC は message assert でなく `fs.readFileSync(sourceMd).not.toContain(href)` を踏む

## 2026-08-15 [20260815-075428-file-viewer-3panes] pdfjs 実レンダを重量ハーネス（notes/outliner standalone = 5 万行級 JS 同居）の TC に置き、フル gate の高並列 CPU 飽和で 120s でも不安定 — 実レンダ検証は軽量ハーネスに集約し面別 TC は配線契約に分離する
- 現象: sidepanel/note 面の PDF 表示 TC（TC-FV-38/39/40 初版）を notes/outliner standalone ハーネス（editor.js + outliner.js 全部入り・5 万行級）上の実レンダ（canvas 出現待ち）で書いた。単独実行は 30s 内で green だが、フル gate（check-known-red = npx playwright test 直・高並列 worker）では CPU 飽和で pdfjs のレンダが 120s 待ちでも不安定に timeout（gate 実測 3 回連続・毎回 fail）。timeout 延長（15s→30s→120s）では解消しなかった
- 原因: 「実レンダで検証」の原則を、レンダ性能がハーネス重量 × 並列負荷に依存することを考慮せず全面 TC に適用した。1 実装 3 マウント（同一 file-viewer.js を 3 面で共用）の構造では、レンダ正しさは軽量ハーネス 1 箇所で検証すれば十分で、面別 TC の固有検証面は「配線」（css 到達・message 中継・destroy 連結）だけだった
- 回避策: (a) **重い実レンダ（pdfjs/canvas/mermaid 等）の検証は最軽量の専用ハーネス 1 箇所に集約**し、共用実装を複数面に載せる構造では面別 TC を「配線契約」（css ルール実在・message 送受・cleanup 連結）に分離する。(b) gate（高並列）と単独実行で挙動が変わる TC は timeout 延長で戦わず検証面を再設計する（延長 2 回で解消しなければ設計問題のシグナル）。(c) ハーネス（standalone build）に新 webview モジュールを組み込むときは、そのモジュールが依存する window グローバル（__pdfExportPost 等の bridge 公開物）のモックも対で追加する（実 bridge はハーネスでは別実装 — 本番だけで成立する縮退経路は TC で silent fail する）。(d) gate runner（run-parallel-tests.sh）にビルド対象を追加し忘れると stale ハーネスで gate だけ fail する — 新 standalone ページ追加時は runner のビルド行も対で更新する
