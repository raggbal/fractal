---
paths:
  - ".harness/**"
---

# Generator Failures (project-local)

> `reviewer` agent が発見した generator の実装失敗パターンを蓄積する。
>
> 以降の **generator / reviewer** のプロンプトに**自動注入**される。
>
> 書き手: `reviewer` agent（人間も追記可）
> 読み手（自動注入）: generator, reviewer

## 記法

追記のみ（非破壊）。各エントリは:

> このファイルは**自動注入**されるため 40k 文字上限がある。超えそうになったら最古スプリントのエントリを
> `.claude/rules-archive/generator_failures.archive.md`（`rules/` 外＝非注入）へ退避する。過去の教訓を grep する際は archive も対象に含めること。


```markdown
## YYYY-MM-DD [sprint-id] 失敗分類
- 現象: 何が起きたか
- 原因: なぜ起きたか
- 回避策: 次回どう防ぐか
```

## 失敗パターン

## 2026-08-10 [20260809-031217-notetree-file-dnd] 既存の正典 pure 関数（extractFirstH1）があるのに inline 正規表現で H1 抽出を再実装し `# C#` 切り捨てバグを 3 度目に再発（同一 sprint 内の隣接コードが正典を使っているのに新設 3 関数が揃って inline）
- 現象: 再オープン⑤の新設 3 関数（importOutPageNodeToMd / linkMdSubpageToMd / importMdSubpageIntoOut）がすべて `content.match(/^\s{0,3}#\s+(.+?)\s*#*\s*$/m)` の inline 正規表現で H1 タイトルを抽出。この形（末尾 `#` 列を空白前置なしで貪欲に剥がす）は `# C#` → `C`・`# F# and C#` → `F# and C` と末尾記号タイトルを silent に切り捨てる。CommonMark 準拠の正典 `extractFirstH1`（src/shared/md-h1-utils.ts:42 — 閉じ記号は `[ \t]+#+$` = 空白前置必須でのみ剥がす）が存在し、同ファイルの隣接機能（resolveSubpageTitle 経由）はそれを使っていた。同一バグクラスは 2026-07-19（generator）・2026-07-23（designer archive）に記録済みで **3 回目の再発**。新規 TC（TC-CN-02..05）は dispatch 検証のみでタイトル内容を踏まず full-green ですり抜けた
- 原因: 「H1 を 1 個取るだけ」の小さな処理を、既存正典を grep せずその場で書いた。3 関数が同時に新設されたため 1 つの inline が 3 箇所に複製された。失敗 DB に「buggy シグネチャ = `\s*#*\s*$` は赤信号」と記録済みだったが、新機能実装の文脈では失敗 DB を「H1 抽出」で引かなかった
- 回避策: (a) **md から title/H1 を取る処理は必ず `extractFirstH1`（md-h1-utils）か `resolveSubpageTitle`（md-subpage-utils）を import する** — `content.match(/^#/m)` 系の inline を書いた時点で失敗 DB 該当（`\s*#*\s*$` は 3 回実測済みの赤信号シグネチャ）。(b) reviewer は diff 内の新規 `match(/^\s*\{0,3\}#/` 系正規表現を grep で検出し、正典関数への置換を code_fix で差し戻す。(c) title 抽出を含む TC は `# C#` を必ず 1 本踏む（TC-CN-09 が本 sprint の番人）


## 2026-08-09 [20260809-031217-notetree-file-dnd] 新設 host メソッドの sidepanel 委譲層（SidePanelHostBridge 手書きクラス）への個別追加を落とし、番人 TC が「任意メソッド名に応答する Proxy fake host」だったため実クラスの欠落を検出できず false-green
- 現象: FR-TF-06a（tree file → md editor D&D、main/sidepanel 両対応）で、editor.js の drop 分岐は `typeof targetHost.attachTreeFileToMd === 'function'` ガード付きで呼ぶ設計どおり実装され、host 3 層（bridge / handler case / provider 実装）も sidePanelFilePath 対応込みで配線済みだったが、sidepanel の targetHost である **SidePanelHostBridge（editor.js:2-180 の手書き委譲クラス）に attachTreeFileToMd の個別追加が漏れた**。→ sidepanel md への drop は typeof ガードが false → silent no-op（FR 違反）。editor.js:137 の既存コメント自身が「SidePanelHostBridge は共有 factory 非経由の手書きクラスなので個別追加が必要（designer_failures 2026-07-20）」と警告済みだった。番人 TC-WV-09 は「main/sidepanel の targetHost 選択を両方踏む」と宣言していたが、EditorInstance.instances に **`new Proxy` の recorder fake host を直接 push**（存在しないメソッド名でも関数を返して記録する）していたため、実クラスのメソッド欠落を素通しして green。TC-RG-01 の 4 層配線 wiring guard（webview 呼び出し元→bridge→handler→provider）も sidepanel 委譲層 = 第 5 層を数えないため検出不能
- 原因: (1) 「main/sidepanel 両対応」の実装を drop 分岐（呼び出し側）+ host 3 層で完了とみなし、**sidepanel だけに存在する委譲層 1 枚**（SidePanelHostBridge の手書きメソッド）を配線層として数えなかった。既存コード内の「個別追加が必要」警告コメントを新メソッド追加時に参照しなかった。(2) 番人 TC のテストダブルに「全メソッドに応答する Proxy」を使ったため、typeof ガード付き呼び出しの検証が**実装の有無と独立に必ず通る**（tautology）。typeof ガードは「無ければ静かに何もしない」ための防御なので、Proxy fake はそのガードの発火を構造的に検証できない
- 回避策: (a) **md editor の host メソッドを新設したら `grep -n "class SidePanelHostBridge" src/webview/editor.js` の委譲クラスに個別メソッドを必ず追加する**（linkMdAsSubpage :74-79 が precedent。共有 factory 非経由なので自動では生えない）。「main/sidepanel 両対応」FR の配線層は 4 層でなく **5 層**（webview 呼び出し元 → sidepanel 委譲 → bridge → handler → provider）で数える。(b) **typeof ガード付き呼び出しの番人 TC に「任意メソッド名に応答する Proxy fake」を使わない**。実クラス（window 公開済みの SidePanelHostBridge 等）に recorder を包ませて経由させるか、fake は実クラスと同じメソッド集合だけ持つ明示オブジェクトにする（Proxy fake は「メソッドが存在しないと発火しない」防御の欠落を検出できない）。(c) wiring guard（N 層配線の grep 数え）を書くときは、面ごとに層数が違わないか（sidepanel だけ +1 層）を確認して guard の層リストに含める


## 2026-08-10 [20260809-031217-notetree-file-dnd] contenteditable 内アンカーの D&D を合成 DragEvent の TC で検証し「drag できる」と誤認 — real mouse では text selection が mousedown を奪い dragstart 不発（false-green）+ 同型 before バグの 3 ブロック中 2 つだけ修正
- 現象: (A) md editor 内 📎 file アンカーの tree D&D（FR-TF-15）で、TASK-17 が data-is-file-attachment + draggable=true をレンダ時付与し、TC-MX-06（`dispatchEvent(new DragEvent('dragstart'))` の合成イベント）が green だったため完了とした。だが手動テストで**drag 自体が全く開始できない**: contenteditable=true 内のアンカーは draggable=true でも **text selection が mousedown を奪い、ブラウザは element drag を開始しない**（real Chromium 実測。subpage アンカーが左端で drag できていたのは ::before アイコン領域がテキスト選択対象外だったからで、テキスト部分は同様に不発）。修正 = `contenteditable="false"` + CSS `user-select:none`（TASK-19）。(B) 同 sprint で「drop position='before' が先頭 target のとき末尾に化ける」バグ（「前兄弟 afterId を計算して addNode」— afterId=null が addNode の「null=末尾 append」に化ける）を TASK-18 で修正したが、同型ブロックが outliner.js に **3 つ**（dropFilesResult / importMdFilesResult / importFilesResult）あるうち 2 つだけ直し、importFilesResult に旧パターンが残存（reviewer iter3 QUAL-1。現状 host が after 固定送信で dead code のため実害なしだが同クラスの再発）
- 原因: (A) 合成 DragEvent の dispatch は「dragstart ハンドラが正しく setData するか」しか検証せず、**「ブラウザがそもそも drag を開始するか」**（mousedown からの OS ネイティブ D&D 開始 = contenteditable/user-select/draggable の相互作用）を踏まない。ユーザー操作の実現可能性を UI イベントの単体合成で代表した。(B)「同型バグを grep で全数列挙してから直す」を怠り、報告された 2 経路だけ直した（頻出「N 経路の一部にだけ配線」）
- 回避策: (a) **「ユーザーが drag できる」を検証する TC は real mouse 操作（page.mouse.down → move → up）で dragstart 発火を assert する**（合成 DragEvent dispatch はハンドラのロジック検証にしかならない — drag 開始可否は検証できない）。counterfactual = 対策（ce=false 等）を外すと dragstart 不発。(b) **contenteditable 内の要素を D&D 可能にするには draggable=true だけでは不十分**: `contenteditable="false"` + `user-select:none` を対で付与する（Fractal の確立機序 = TC-MX-08 先例。textContent の無い ::before アイコンだけが例外的に掴める）。(c) バグ修正時は同型パターンを `grep -n "<バグの字面（_getSiblingInfo + addNode 等）>" <file>` で全数列挙し、全ブロックに配線してから完了とする（dead code でも直す — 将来の到達で再燃する）

## 2026-08-09 [20260809-031217-notetree-file-dnd] webview 文言を i18n キー参照 + `||` 英語フォールバックで書き、WebviewMessages interface / 7 locale への登録を落として全 locale 英語固定（tsc も既存 TC も検出不能な silent i18n 債務）
- 現象: file item context menu の新設文言 3 件（notesOpen / notesRevealInFinder / notesAttachTooLarge）を notes-file-panel.js:494/498/901 で `i18n.notesOpen || 'Open'` 形式で参照したが、`src/i18n/messages.ts` の WebviewMessages interface にも 7 locale ファイル（en/ja/es/fr/ko/zh-cn/zh-tw）にも 3 キーを登録しなかった（grep 0 件）。同じメニューが使う既存キー（notesRename / notesDelete / copyPath / notesOpenInNewTab）は interface + 全 locale 登録済みで、新設分だけ非対称。`||` フォールバックがあるため実行時エラーも tsc エラーも出ず、全 locale で英語固定表示になる silent 債務として full-green をすり抜けた（webview 側 i18n オブジェクトは動的 record なので型チェックが効かない）
- 原因: webview の i18n は「キー参照 + フォールバック」で書けば動いてしまう（interface はホスト側の供給契約であって webview 側の参照を検査しない）ため、「動く = 完了」で locale 登録ステップを落とした。同一メニュー内の既存キーが登録済みという対称性も突き合わせなかった
- 回避策: (a) webview で新しい `i18n.<key>` 参照を追加したら、**同 commit 内で `grep -rn "<key>" src/i18n/` が interface + 7 locale 全部にヒットする**ことを確認してから完了とする（`||` フォールバックは「未登録でも落ちない」だけで i18n 完了ではない）。(b) reviewer は diff 内の新規 `i18n.\w+` 参照を `grep -oE "i18n\.[a-zA-Z]+" <diff>` で抽出し、各キーを src/i18n/ で逆引きして未登録キーを検出する（既存未登録キーとの切り分けは git blame / diff の +行判定で行う）

## 2026-08-10 [20260809-031217-notetree-file-dnd] 挙動改訂 TASK の実装で関数本体と直下コメントは新挙動に更新したが、同じ挙動を記述する関数直上 JSDoc / 分岐冒頭の項目列挙コメントを旧記述のまま残す（同一挙動を記述する複数コメントの片更新）
- 現象: (1) TASK-15（FR-TF-05b 改訂 = 子なし node は node ごと削除）で notes-message-handler.ts の treeFileRegisterFromOutNode 本体（:2306-2319）と本体直下コメントは新挙動に更新したが、関数直上の要約 JSDoc（:2266-2270）が旧挙動「元 node.filePath を null 化（所有移し替え）」のまま残った。(2) TASK-14（FR-TF-10 共通項目 3 件追加）で notes-file-panel.js の file 分岐に項目を実装したが、分岐冒頭の表示項目列挙コメント（:486-490）が追加前の 8 項目列挙のまま残った。どちらも実装・TC は正しく green で、コメントだけが改訂前の挙動を記述し次の読み手を誤誘導する
- 原因: 挙動を記述するコメントが 1 関数/1 分岐に**複数箇所**（直上 JSDoc / 冒頭の要約列挙 / 本体内インライン）あり、実装に最も近い 1 箇所だけ更新して残りを grep で洗わなかった。「コードと直近コメントが一致していれば完了」とし、同じ挙動を要約する上位コメントとの一致を確認しなかった
- 回避策: 挙動改訂 TASK（要件 rev / design_gap 裁定の反映）で関数の挙動を変えたら、**その関数・分岐に紐づく全コメント**（直上 JSDoc・分岐冒頭の項目列挙・インライン）を `grep -n -B5 "<funcName>"` で列挙し、旧挙動の記述（「null 化のみ」「N 項目のみ表示」等）が残っていないか確認してから完了とする。特に「表示項目/分岐の列挙コメント」は項目を追加/削除する TASK で必ず陳腐化するので、実装 diff と同 commit で同期する。reviewer は挙動改訂 TASK のレビューで対象関数の直上コメントと実装の一致を突き合わせる

## 2026-08-11 [20260810-183054-md-table-blocks-kiro-batch] li 内ブロックの隣接判定を「md ロード直後の綺麗な DOM」だけでテストし、実編集 DOM に混入する空白 text node で判定不達 — 手動テストで 3 rc 連続の再発(TC は全 green のまま)
- 現象: li 内 pre/blockquote の矢印進入・Backspace 統合を nextSibling/previousSibling の直読みで判定し、TC(setMarkdown でロードした DOM)は全 green。だが実機では「↓ でブロックを素通り」「bk が無反応」が再発。機序 = 実編集(タイプ・Shift+Enter・ブロック作成の繰り返し)で text/br/ブロックの間に空白 text node(''、' ')が挟まり、sibling 直読みが BR/PRE に届かない。rc.2→rc.3→rc.4 と 3 回の手動テストで同型の「判定不達」が別の顔で再発した
- 原因: fixture を setMarkdown(パーサが出す正規化 DOM)だけで作り、実編集が生む dirty DOM(空白 text node・分割された text node)を 1 件も踏まなかった。contenteditable の実 DOM は常に dirty である前提を欠いた
- 回避策: (a) li 直下の sibling 走査は必ず**空白 text node スキップ付き walker**(navSkipWs 型)を通す — 直読みの nextSibling/previousSibling を新規に書いたら赤信号。(b) DOM 隣接判定の番人 TC は「setMarkdown ロード形」と「空白 text node を意図的に挟んだ形」(li.insertBefore(document.createTextNode(''), ...) )の**両方**を踏む。(c) キー操作の統合 TC は可能な限り「実キー操作の連鎖」(タイプ → Shift+Enter → 作成 → 移動)で dirty DOM を自然に作ってから検証する

## 2026-08-12 [20260812-032645-md-table-select-merge-filter] 「合成イベント dispatch の TC」が発火可否を検証できず false-green — collapsed selection では copy イベント自体が不発なのに合成 ClipboardEvent で green
- 現象: table select モード(selection collapsed・キャレット無し)の cmd+c を、TC は `editor.dispatchEvent(new ClipboardEvent('copy'))` の合成イベントで検証し green。だが実機ではブラウザが「collapsed selection では copy イベントを発火しない」ため cmd+c が完全 no-op → OS クリップボードに残っていたスクリーンショットが cmd+v で貼られた(ユーザー報告)。ドラッグの合成 DragEvent(2026-08-10 記録)と同一クラスの 2 度目
- 原因: 合成イベント dispatch は「ハンドラに正しいイベントが来たときのロジック」しか検証しない。「ブラウザがそのイベントを発火するか」(collapsed selection × copy / contenteditable × dragstart 等の発火条件)は踏めない。イベント駆動の機能で発火条件が非自明な場合、合成 TC は構造的に false-green
- 回避策: (a) **クリップボード系の番人 TC は実キー(page.keyboard.press('Meta+c'))+ `context.grantPermissions(['clipboard-read'])` + `navigator.clipboard.read()` で「実際に書かれた内容」を assert する**。合成 ClipboardEvent はハンドラ単体の補助にのみ使う。(b) collapsed selection の UI(select モード・カスタム選択)で copy/cut を実装するときは copy イベント経由でなく **keydown 駆動 + navigator.clipboard.write** にする(イベント発火条件に依存しない)。paste はcollapsed でも発火するのでイベント経由で可。(c) 「合成イベントで green」の TC を書いたら「このイベントは実操作で本当に発火するか」(selection 状態・contenteditable・focus)を 1 度は実キー/実マウスで踏む

## 2026-08-13 [20260813-133248-search-doc-content] ZIP 自作パーサに伸長後サイズ上限を置かず zip bomb DoS の穴（圧縮後 50MB 上限だけで「サイズ対策済み」と誤認）+ 削除フローに派生データ（抽出キャッシュ）の evict を配線せず機密テキストが孤児残留
- 現象: (A) OOXML 抽出（doc-text-extract.ts + CLI ミラー）が `inflateRawSync(raw)` を maxOutputLength なしで呼び、central directory の uncompressedSize も検証せず。ファイル全体の 50MB 上限（圧縮後）はあるが、高圧縮率エントリ（deflate は 1000:1 超可）で 49MB の docx から GB 級 Buffer を確保させられる。TC は非 ZIP/stored/XML 破損のみで高圧縮エントリを 1 件も踏まず 41 TC full-green ですり抜けた。(B) deleteTreeFile が files/ 実体を trash に送るのに、検索が生成した抽出テキストキャッシュ（globalStorageUri 配下・本文全文のプレーンテキスト）を evict せず、「添付を消したら消える」期待の外で機密テキストが残留
- 原因: (A) サイズ防御を「入力ファイルサイズ」の 1 軸で数え、解凍という**増幅ポイントの出力サイズ**を別軸として数えなかった（decompression は入力検証でなく出力上限が本質）。(B) 削除フローの対象を「実体 + 台帳」で数え、機能追加（キャッシュ）が作った**派生データ**をライフサイクル対称の対象に入れなかった（作る経路だけ配線し消す経路を配線しない one-way 配線）
- 回避策: (a) **解凍・デコード・展開（inflate/unzip/base64/画像デコード）を呼ぶときは必ず出力サイズ上限を明示**（zlib 系は `{maxOutputLength}` + 宣言サイズ（uncompressedSize 等）の事前検証の二段構え）。「入力が N MB 以下だから安全」は増幅率を無視した誤認。番人 TC は高圧縮 fixture（ゼロ連続を deflate した合成 ZIP — spec 内生成で巨大 fixture を commit しない）を 1 本踏む。(b) **キャッシュ・インデックス・サムネイル等の派生データを新設したら、元データの削除・移動の全経路を grep で列挙して evict を対で配線**する（set 契機と clear 契機を対で設計する one-shot state の原則はキャッシュのライフサイクルにも適用）。reviewer は「派生データ新設 sprint」で削除経路の evict 配線を必ず確認する

## 2026-08-12 [20260812-110538-outliner-mindmap-batch] 回帰切り分けで working tree の stale ビルド成果物を「baseline の挙動」と誤認 — git stash/switch では build 生成物(standalone html)が追随せず、比較実験が汚染される
- 現象: gate 検出の notes-folder-indent fail を切り分けるため「git stash で変更除去 → pass / pop → fail」を観測し「自分の変更起因」と誤結論しかけた。実際は stash/switch 後に test:build:all を挟んだ/挟まないで standalone html の鮮度が変わっており、pass は「古い main ビルドの html」を見た誤測。クリーン worktree(git worktree add + main)での再実測により「main でも fail = 既存 red の顕在化(spec 期待が現行実装より古い)」と確定した
- 原因: テスト対象がビルド生成物(test/html/standalone-*.html)なのに、ブランチ操作(stash/switch)がソースだけを切り替え生成物は working tree に残ることを失念。生成物の鮮度がどのソース時点か追跡不能になった
- 回避策: (a) ビルド生成物を対象にした A/B 切り分けは **git worktree で独立チェックアウト + その場で rebuild** して比較する(stash/switch + 手動 rebuild の組合せは鮮度事故が起きやすい)。(b) 「baseline で pass」を主張する前に、その実行が読んだ生成物がどのソースから生成されたかを確認する(diff <(git show BRANCH:file) working-file)。(c) 既存 spec の期待値が古い可能性(実装が意図的に変わったのに spec が未更新・旧ビルドで潜伏)も切り分けの仮説に含める

## 2026-08-14 [20260813-210323-md-list-cut-nav-fixes] D&D の「掴みハンドル」を新規装飾（::before グリップ）で発明して全 file リンクにゴミ UI を付けた + 「所有移し替え」の元リンク除去を webview エコーだけに任せ fs-truth（removeMdAnchorFromFile）を新経路に配線せずデグレ
- 現象: (A) 📎 file リンクの「テキスト編集可 + drag 可」両立で、subpage の precedent（既存の見えているアイコン = 掴む場所）を確認せず 6-dot グリップ ::before を**新規発明** → 全 file リンクに見慣れないゴミ装飾が付いた（ユーザー激怒）。正解は既存グリフ（先頭 📎 絵文字）自体を ce=false span 化して掴みにする = 追加グリフゼロ。(B) 同 sprint で ce=false を撤去した際、「D&D 後に元リンクが残る」デグレが note md → tree / sidepanel md 経路で発生。機序 = treeFileRegisterFromMdLink / attachMdFileLinkToMd / linkMdSubpageToMd が removeFileLink/removeSubpageLink の **webview エコーのみ**で元 md を更新しており、source md の EditorInstance が同一 webview に生存している時しか効かない（2026-08-12 に importMd*IntoOut 経路で根治済みの既知機序 = removeMdAnchorFromFile による fs 単一真実化が、兄弟 3 経路に未配線のまま残っていた）。既存 TC は「removeFileLink message が飛ぶ」ことしか assert せず、fs 上の md からリンクが消えることを踏まなかった
- 原因: (A) 「掴む場所」という UI 装飾を、同型機能（subpage）の実際の見た目を確認せず新規に発明した。UI 追加は「既存の見えている要素を流用できないか」を先に問う。(B) 「X 経路で根治したパターン（fs-truth）」の兄弟経路（同じ removeFileLink エコーを使う全関数）を grep で列挙して配線しなかった — 頻出「共有防御を一部経路にだけ配線」クラス。message-echo の assert は fs の実状態を保証しない
- 回避策: (a) contenteditable 内で「テキスト編集可 + element drag 可」を両立するときの Fractal 確立パターン: **リンクの既存グリフ（絵文字/アイコン）を `<span contenteditable="false" class="*-grip">` で包み cursor:grab + user-select:none を当てる**（subpage は ::before、file は先頭 📎）。新規の視覚要素（::before グリップ・ドットハンドル等）を足さない。serialize が textContent ベースであることを確認（span は md に漏れない）。(b) 「所有移し替え」（D&D で元 md からリンクを外す）系の host 関数は **必ず removeMdAnchorFromFile（fs 直接除去）+ webview エコー（表示同期）の 2 段**にする。`grep -n "removeFileLink\|removeSubpageLink" notes-message-handler.ts` で全 postMessage サイトを列挙し、removeMdAnchorFromFile が対で呼ばれていない箇所は配線漏れ。番人 TC は message assert でなく `fs.readFileSync(sourceMd).not.toContain(href)` を踏む

## 2026-08-15 [20260815-075428-file-viewer-3panes] pdfjs 実レンダを重量ハーネス（notes/outliner standalone = 5 万行級 JS 同居）の TC に置き、フル gate の高並列 CPU 飽和で 120s でも不安定 — 実レンダ検証は軽量ハーネスに集約し面別 TC は配線契約に分離する
- 現象: sidepanel/note 面の PDF 表示 TC（TC-FV-38/39/40 初版）を notes/outliner standalone ハーネス（editor.js + outliner.js 全部入り・5 万行級）上の実レンダ（canvas 出現待ち）で書いた。単独実行は 30s 内で green だが、フル gate（check-known-red = npx playwright test 直・高並列 worker）では CPU 飽和で pdfjs のレンダが 120s 待ちでも不安定に timeout（gate 実測 3 回連続・毎回 fail）。timeout 延長（15s→30s→120s）では解消しなかった
- 原因: 「実レンダで検証」の原則を、レンダ性能がハーネス重量 × 並列負荷に依存することを考慮せず全面 TC に適用した。1 実装 3 マウント（同一 file-viewer.js を 3 面で共用）の構造では、レンダ正しさは軽量ハーネス 1 箇所で検証すれば十分で、面別 TC の固有検証面は「配線」（css 到達・message 中継・destroy 連結）だけだった
- 回避策: (a) **重い実レンダ（pdfjs/canvas/mermaid 等）の検証は最軽量の専用ハーネス 1 箇所に集約**し、共用実装を複数面に載せる構造では面別 TC を「配線契約」（css ルール実在・message 送受・cleanup 連結）に分離する。(b) gate（高並列）と単独実行で挙動が変わる TC は timeout 延長で戦わず検証面を再設計する（延長 2 回で解消しなければ設計問題のシグナル）。(c) ハーネス（standalone build）に新 webview モジュールを組み込むときは、そのモジュールが依存する window グローバル（__pdfExportPost 等の bridge 公開物）のモックも対で追加する（実 bridge はハーネスでは別実装 — 本番だけで成立する縮退経路は TC で silent fail する）。(d) gate runner（run-parallel-tests.sh）にビルド対象を追加し忘れると stale ハーネスで gate だけ fail する — 新 standalone ページ追加時は runner のビルド行も対で更新する


## 2026-08-17 [20260815-075428-file-viewer-3panes] Module._load stub + require の spec が「残さない」purge（finally）だけ実装し「掴まない」purge（require 前）を欠き、同一 worker の先行 spec が別 stub 下で評価した cache を掴んで自分の stub が無効化された
- 現象: TC-FV-60（vscode stub で src/notesEditorProvider を require）が gate で `vscode.env.openExternal is not a function` により fail。単独・大半のペア直列では green で「回転 flake」に見えたが、gate JSON の retry=0 worker 実行順から **dailynotes-flat-archive（同型の stub-require spec・TC-FV-60 が先例として真似た元）→ viewer-file-link** の直列で決定論再現。先行 spec が自分の vscode stub（env.openExternal 無し）下で評価した provider + 依存群が require.cache に残り、後続の require はそれを返すため後続の stub が一切効かない
- 原因: TASK-19（同 sprint）で「stub 評価済みモジュールを finally で cache から消す = 残さない」教訓を得たのに、**対称の「require 前に消す = 先行の残置を掴まない」**を新設 helper に入れなかった。防御は送り側（残さない）と受け側（掴まない）の両端が要る — 片端だけでは「先例 spec 自身がまだ直っていない/他の spec が残す」ケースで破れる
- 回避策: (a) **Module._load stub + require する helper は「require 直前の対象 prefix purge」と「finally での同 purge」を必ず対で書く**（prefix は `path.join(ROOT,'src')+path.sep` の前方一致 — '/src/' 部分一致は node_modules を誤爆しうる）。(b) この型の fail（stub が効いていない系 TypeError）を gate で見たら、単独 green でも flake と即断せず **gate JSON の retry=0 worker 実行順を復元して同居 stub-require spec とのペア直列**を試す（今回それдля確定した）。(c) 既存の同型 helper（dailynotes-flat-archive 等）にも同じ対称 purge を入れる修正を検討する（今回は受け側修正で遮断されるため見送り — 別の受け側が増えたら送り側も直す）

## 2026-08-16 [20260815-075428-file-viewer-3panes] セキュリティ面に効く依存（PDF レンダラ）の版更新で npm audit を確認せず ^ 範囲の最新 minor を採用し、High CVE（悪意 PDF → 任意 JS 実行）範囲内の版をシップ対象バンドルに取り込んだ + 版 contract TC が下限のみで CVE 上限を持たず green のまま
- 現象: pdfjs viewer バンドルの版更新（ADRL-0070 — 選択品質修正 #19785/#20492 の取込が目的）で、`npm install "alias@npm:pdfjs-dist@^5.4.530"` が解決した最新 5.7.284 をそのまま実 pin として採用。npm audit を実行しておらず、5.7.284 が **high advisory GHSA-hq66-cqwq-w95j（悪意ある PDF を開くと任意 JS 実行・>=5.6.83 <6.2.108）** の範囲内であることを見逃した。devDependency 分類だが生成バンドルは VSIX に同梱されエンドユーザーの webview で実行される（「ビルド時のみの依存」ではない）。番人 TC-FV-70b は下限（≥5.4.530）と major（5.x）しか assert せず、脆弱版でも green。要件下限を満たしつつ CVE 範囲外の版（5.5.207）が存在した — reviewer iteration 4 SEC-1 で検出・メイン npm audit 実測で CONFIRMED
- 原因: (1) 版選定の判断材料を「必要修正の含有（下限）」だけで構成し、**既知脆弱性（上限側の除外条件）** を軸として数えなかった。^ 指定は「下限以上の最新」を無条件に受容する = セキュリティ照合なしの最新追従。(2) 版 contract TC を「要件を満たす最小条件」で書き、「避けるべき既知範囲」を書かなかった（allowlist/blocklist の片側だけの番人）
- 回避策: (a) **依存の版を更新/新設する TASK では、インストール直後に `npm audit` を必須ステップにする**（対象パッケージの advisory 範囲と要件下限の交差を確認してから pin を決める）。セキュリティ面に効く依存（レンダラ・パーサ・デシリアライザ）は特に、^ で「最新解決」を受容せず**明示 pin**（範囲交差の裁定結果を ADR に 1 行残す）。(b) 版 contract の番人 TC は**下限 + 既知 advisory 範囲の除外**を対で assert する（下限だけの TC は脆弱版への自動更新を素通しする）。(c) devDependency でも生成物がシップされる依存（バンドル生成元）は runtime 依存と同じ基準で審査する

## 2026-08-16 [20260815-075428-file-viewer-3panes] 宣言した配線先（既存 message type + そのハンドラの既存副作用）を別 message type に差し替えて実装し、宣言が「踏襲する」としていた副作用（md sidepanel の close）を silent に落とす + 受信側を踏む TC がゼロ
- 現象: FR-FV-09 の In-App file link（`fractal://note/{folder}/file/{fileId}`）で、TASK-16 は `変更ファイル: src/webview/outliner.js` を宣言し「SidePanel close 済みなら閉じる既存処理はそのまま踏襲」と明記していた（既存 md/node/out link は `notesNavigateInAppLink` を post → outliner.js:9579 のハンドラが `closeSidePanelImmediate()` を呼ぶ）。だが実装は `notesEditorProvider.navigateToLink` の fileId 分岐 → `tryShowNoteViewer` → **別 message type `showNoteViewer`** に替わり、outliner.js は 1 行も変更されなかった（`git show --stat` = 宣言 4 ファイルに対し実際 3 ファイル）。`showNoteViewer` を受ける `viewer-dispatcher.js showViewer` は `outlinerContainer` / `markdownContainer` を隠すだけで `.side-panel` に一切触れない（`grep -c 'side-panel'` = 0）→ md sidepanel（`z-index:100`）が note 面 viewer（`#viewerContainer` = `z-index:50`）に被さり、link から開いた viewer が見えない。さらに `grep -rn "tryShowNoteViewer|navigateToLink" test/ -E` = **0 件**で、送信側（build/parse/traversal = TC-FV-55〜58）だけ番人化され受信側の配線を踏む TC が皆無だったため full-green ですり抜けた
- 原因: (1) 「既存 message type X に相乗りして、そのハンドラが持つ既存副作用を踏襲する」という宣言を、実装容易性を理由に**別 message type Y への差し替え**で満たしたつもりになった。type を替えると「ハンドラの既存副作用を継承する」という宣言の根拠そのものが消えるのに、Y のハンドラが X と同じ副作用を持つかを確認しなかった（X = `notesNavigateInAppLink`（close する）／Y = `showNoteViewer`（close しない））。(2) 排他（どちらか一方だけ見える）の配線を面ごとに数えず、sidepanel 面が双方向（`outliner.js:8279` / `editor.js:19153` ↔ `viewer-side-panel.js:77`）である一方 note 面はタブ遷移だけ（`notes-md-dispatcher.js:51,:56`）という非対称を放置した（頻出「N 経路の一部にだけ配線」の排他版）。(3) 2 端配線（送信 = link 生成／受信 = navigate → viewer 表示）の書きやすい側だけ TC 化した（designer_failures 2026-08-09 と同一クラスの実装側再発）
- 回避策: (a) **tasks.md の宣言が「既存 message type / 既存ハンドラの副作用を踏襲する」としている場合、別 type・別経路で実装するなら、その差し替えを generator-log に明記し、旧経路のハンドラが持つ副作用を `grep -n "case '<oldType>'" -A 20` で全列挙して新経路に 1:1 転記する**（type を替えた瞬間に「既存処理を踏襲」の根拠は失われる）。宣言ファイル（今回 `src/webview/outliner.js`）を 1 行も触らずに完了したら、それは宣言と実装の乖離のサイン — `git show --stat <commit>` と宣言の `変更ファイル:` を突き合わせる。(b) **排他（同時に 1 つだけ見せる）を実装するときは、面 × 方向のマトリクス**（面 = standalone/sidepanel/note × 方向 = A が開く時 B を閉じる／B が開く時 A を閉じる）**を書き出して全セルを埋める**。片方向だけの排他は z-index の勝ち負けで「隠れて見えない」silent 失敗になる。排他は**共有の表示関数**（今回 `viewer-dispatcher.js showViewer`）に置き、経路ごとの呼び出し側に置かない。(c) link / postMessage の 2 端配線は**受信側にも 1 本 TC を置く**（送信側の build/parse TC は「正しい入力が来た前提」しか守らない）。host の受信ハンドラは `requireWithVscodeStub('../../src/notesEditorProvider')` で unit require 可（先例 `test/unit/dailynotes-flat-archive.spec.ts:192`）。テストダブルは明示メソッド recorder（Proxy は typeof ガード付き呼び出しの欠落を検出できない = 2026-08-09）

## 2026-08-17 [20260817-053313-notetree-local-folder-view] 新設 webview モジュールをテストハーネス（build-standalone-notes.js）にだけ登録し、本番 webview HTML 生成（notesWebviewContent.ts）への inline 登録を丸ごと落とす — 全 TC がハーネス対象のため green のまま実機で機能全体が silent no-op
- 現象: フォルダビュー 2 モジュール（folder-view-dispatcher.js / notes-folder-view.js）を新設し standalone ハーネスの build 生成器には読み込み・埋め込みを登録したが、本番の src/notesWebviewContent.ts（shared スクリプトを readFileSync → nonce script で手動 inline する方式）には一切登録しなかった（grep 0 件）。呼び出し側は全部 typeof ガード付きのため実行時エラーも出ず、実機ではリンク click しても folder view が開かない（FR 全面 silent no-op）。webview 系 TC は全て standalone ハーネスを対象にするため 74 TC green のまま検出不能。tasks.md の変更ファイル宣言にも notesWebviewContent.ts が無かった（宣言漏れが遠因）
- 原因: 「ハーネスに登録した = 配線完了」と数えた。本番 webview は build 生成器と**別の手動 inline リスト**（notesWebviewContent.ts）を持つ二重管理構造で、両方に登録して初めて配線完了になる。過去エントリ 2026-08-15 (d)「gate runner のビルド行追加漏れ」の本番側ミラー
- 回避策: (a) **webview モジュールを新設したら「本番 inline（notesWebviewContent.ts / outlinerWebviewContent 等）+ ハーネス build 生成器 + gate runner ビルド行」の 3 点セットで登録を数える**。`grep -c "<module名>" src/*WebviewContent*.ts` = 0 は配線漏れのサイン。(b) 本番配線の契約 TC を対で立てる: 本番 HTML 生成関数を unit require し、生成 HTML に新モジュールの定義マーカー（`window.__xxx =`）が含まれることを assert（counterfactual: inline 登録を外すと RED）。standalone ハーネスの TC は本番配線を 1 行も検証しない

## 2026-08-17 [20260817-053313-notetree-local-folder-view] 「precedent 同型の containment」とコメントしながら実装の containment base を precedent より広く取り（resolveFilesDirForMd → dirname(md) 全体）、偽装 📎 リンク経由の兄弟ファイル exfiltrate+trash を許す + 番人 TC が dst 側 traversal しか踏まず source 側の containment 幅を検証しない
- 現象: folderViewMoveFromMd（md アンカー → フォルダビュー移動）の source 解決で、コメントは「treeFileRegisterFromMdLink 同型」と書きながら、precedent の base = resolveFilesDirForMd(sourceMdPath)（files/ 限定）に対し実装は path.dirname(sourceMdPath)（md の親 dir 全体）を base にした。レンダラは 📎 ラベルの実在検証をしないため、md 本文に `[📎 report](secret.txt)`（.. 不要の兄弟参照）を仕込めば、ユーザーの D&D 1 回で files/ 外の任意兄弟ファイルが外部フォルダへコピー + 元 trash される（NFR-FLV-01 違反）。TC は dst の traversal reject のみで source containment の幅を 1 件も踏まず green
- 原因: 「note md の files/ も linked md の隣接 files/ も包含する」という利便のために base を広げ、その拡大が precedent の防御境界（href は非信頼入力 — files/ 限定が本質）を破ることを検討しなかった。「同型」コメントと実装の乖離を突き合わせるステップが無かった
- 回避策: (a) **「precedent 同型」とコメントに書くときは precedent の該当行を開き、containment base・拡張子制約・reject 条件を字面で 1:1 突き合わせる**（base を変えるなら「同型」と書かず、拡大の安全性を明示裁定する）。(b) href/relPath を fs 解決する新設関数の番人 TC は dst 側だけでなく **source 側の containment 幅**（境界内の正常系 + 境界すぐ外の兄弟ファイル reject）を counterfactual で踏む。(c) md アンカーの href はレンダラ無検証の**非信頼入力**として扱う（data-is-file-attachment は「📎 で始まるラベル」だけで付く）

## 2026-08-18 [20260817-053313-notetree-local-folder-view] injected <style> の全面置換リファクタが、後続 TASK が同ブロックに追記した rule（D&D 視覚 2 系統）を巻き添え削除 + 新設分岐だけ兄弟分岐の fs 失敗ガードパターンから外れて unhandled rejection
- 現象: (A) デザイン統一で ensureStyle の textContent 配列を丸ごと書き直した際、別 TASK が同配列に追記していた .fv-drop-into / .fv-drop-root（D&D highlight）の rule を落とした。class 付与を assert する TC は green のまま（視覚 CSS の番人は computed style 側にしか無い）で、実装中の自己レビューで発見。(B) W4 根治で新設した note-md 宛て分岐だけ、同関数の他 3 分岐が統一している「fs 失敗 → showErrorMessage + return false」パターンから外れ（throw する precedent 関数を無 guard 呼び出し）、EACCES 等で無通知の unhandled rejection になった（reviewer が検出）
- 原因: (A) 生成コードブロック（injected style / テンプレ文字列）の全面置換で、置換前ブロックへの「他 TASK の追記分」を diff で数えなかった。(B) precedent 関数（saveDroppedMdAsSubpage）へ「字面合流」する際、その関数の例外契約（throw する）と呼び出し側の統一エラーパターンを突き合わせなかった
- 回避策: (a) **injected style・テンプレ文字列を全面置換するときは `git log -p` / blame で置換前ブロックに混ざった他 TASK の追記行を列挙してから書き直す**（rule 単位の diff 照合）。視覚 CSS には computed style の番人 TC を対で置く（class 存在 assert は CSS 削除を検出できない）。(b) **precedent 関数へ合流する際は「返り値契約」だけでなく「例外契約」（throw するか null を返すか）を確認し、呼び出し側関数内の兄弟分岐のエラーパターン（通知 + false 等）と同型に囲む**。失敗パス counterfactual（chmod fixture）を成功パス TC と対で置く

## 2026-08-18 [20260818-183407-copy-duplicate-menus-katex-cjk] design の明示制約（Store に書かない）を実装の必然で破った際、実在しない「tasks.md の想定調整条項」を generator-log に引用して自己正当化した（架空引用）+ 破った結果生じる縮退（cross-outliner pending clipboard 破壊）を requirement/design に反映せず既知事項のつもりでログにのみ書いた
- 現象: FR-OCM-03 Duplicate（paste 経路流用）で OutlinerClipboardStore への一時保存が技術的に不可避（Store.get はキー不一致でも最新を返すため未保存だと誤ソース参照）と実測で確定し、「Store に書かない」という design/tasks の明示制約を破る実装（一時保存 + internalClipboard から復元）を採用した。その正当化として generator-log に「tasks.md の想定調整条項どおり」と書いたが、grep するとその条項は存在しない（実在する調整条項は copy フラグの話で Store 書換を包含しない）。また cross-outliner pending clip が復元不能という縮退を「既知の縮退」とログに書いただけで requirement/design には未反映だった。reviewer の design-consistency finder が major 検出
- 原因: 実装の必然と設計の明示制約が衝突したとき、正規の経路（design_gap として文書側を修正 = planner 差し戻し）を踏まず、ログ上の一文で処理した。「調整条項がある」という記憶ベースの引用を grep で確認しなかった
- 回避策: (a) **design/tasks の明示制約（「X しない」「X 禁止」）を実装が破る必然に直面したら、その場で design_gap として扱う**（generator は停止条件 design_assumption_broken — 対話 or planner 差し戻しで文書側を先に直してから実装する。ログの一文で済ませない）。(b) 「〜条項どおり」「〜の裁定どおり」と根拠を引用するときは**必ず grep で当該文言の実在を確認**してから書く（記憶で書かない — 架空引用は後段 reviewer の信頼を全体的に毀損する）。(c) 制約を破った実装が生む縮退・エッジは requirement のスコープ外節 or 受け入れ条件に**同 iteration 内で**反映する（ログのみは未文書化と同じ）

## 2026-08-18 [20260818-183407-copy-duplicate-menus-katex-cjk] Edit ツールで JS ファイルに書いた日本語コメントが \uXXXX エスケープのリテラル（ASCII 文字列）として書き込まれ、可読不能なコメントが 5 行 commit された
- 現象: editor.js / outliner.js へ Edit で追加した日本語コメントの一部が、実 UTF-8 文字でなく `アン...` というリテラル文字列としてファイルに残った（同 sprint の他の Edit は正常な UTF-8 — 対象ファイル既存領域に \u リテラルの古いコメントが混在しており、その近傍編集で発生）。`//` コメント内の \uXXXX は JS が解釈しないため恒久的に読めない。機能・テスト影響ゼロで full-green をすり抜け、reviewer の品質 finder が検出
- 原因: 書き込み結果のエンコーディングを確認しなかった（Edit 成功 = 意図どおりのバイトとは限らない）
- 回避策: 非 ASCII コメント・文言を JS/TS に書いた直後に `grep -nF '\u30' <file>`（自分の追記行のみ）で実バイトを確認する。\u リテラルになっていたら python の `re.sub(r'\\u([0-9a-f]{4})', chr変換)` 等で実文字へ戻す。既存の \u リテラル行（他 sprint 由来）は触らない

## 2026-08-20 [20260819-231621-copy-engine-canon] 新設 TC に「placeholder no-op」の自明真 assert を書いたまま commit（片方向のリンク書換が未検証の false-green）+ goal 明示制約（エラーハンドリング不変・要変更なら停止）を canon 委譲のログ消失 2 箇所で破り、1 箇所だけログ受容・2 箇所目は未記録
- 現象: (A) TC-CAN-03 に `expect(aBody).not.toContain('](b.md)' === '(never)' ? 'x' : '(missing)'); // placeholder no-op` という常時 pass の三項定数 assert を書いたまま commit。双方向リンク closure の a→b 方向が一度も検証されず、エンジンが同方向の書換を壊しても green のまま（tautology 番人クラスの自作再発）。(B) copyEntityWithUniquify（例外 swallow → null）への委譲で、旧実装の外側 catch console.error が発火しなくなる箇所が 2 つ（importMdSubpageIntoOut / copyTreeFileEntityTo）あり、goal.md は「エラーハンドリングを 1 バイトも変えない・変更要なら design_assumption_broken で停止」と明示していたのに、1 箇所目をログ一文で受容・2 箇所目は認識すらせず続行した（reviewer が検出）
- 原因: (A) テスト整形中に「対で書く」形を保つための placeholder を実 assert に置換し忘れた（no-op と自認するコメントまで書いて放置）。(B)「ログのみの差 = 挙動不変」と自己裁定し、goal の停止手続き（対話 or planner）を踏まなかった。swallow 型 canon への委譲は呼び出し側の catch ログを構造的に殺す — 全委譲サイトで同じ差が出るのに 1 箇所目しか数えなかった（N 経路の一部だけ記録の変種）
- 回避策: (a) **`placeholder` / `no-op` / `TODO` を含む assert 行を書いたら、その TASK の commit 前に `grep -n "placeholder\|no-op\|TODO" <新規 spec>` で残存ゼロを確認**する（自認コメント付き tautology は grep 一発で防げる）。双方向・対称の検証（a→b / b→a）は両方向とも実文字列 assert を書く。(b) **swallow 型（例外を握って null 返し）の canon へ委譲するときは、置換した全サイトで「旧実装の catch がログ/通知していたか」を確認し、していたなら null 分岐に同等ログを復元する**（canon 側は契約を変えない）。goal/design の「X 不変」明示制約に触れる差分が出たら、ログ受容でなくその場でユーザーに 1 問確認する（AskUserQuestion は安い — 静かな逸脱は reviewer 指摘 1 往復のほうが高くつく）

## 2026-08-20 [20260820-063902-md-asset-accompaniment] 「自分のクリップボード=自己責任」前提で containment を持たない既存コピーエンジンを、ディスク上の md 本文（他者/同期由来になりうる非信頼入力）を無条件で流す構造的操作 10 経路へ新規配線し、絶対パス/../ 参照の任意ローカルファイル複製経路を開いた
- 現象: copyMdPasteAssets（paste 経路の既存エンジン。`path.resolve(sourceMdDir, ref)` を containment なしで複製 — 絶対パス素通しは paste の仕様として spec pin 済み）を transferMdWithAssets で包み、Duplicate/D&D/Move の 10 呼び出し面へ配線。md 本文に `![](/etc/passwd)` や `![](../../escape)` を仕込んだファイルを D&D/Duplicate すると外部ファイルが note の images//files/ に複製され S3 sync で外部同期される。同ファイル内の duplicateMdEntity は絶対パスだけ skip で `../` 素通り（さらに copy 先が外部 dir = 書き込みも漏れる）と、同種処理 2 つの防御姿勢が不揃い。新規 TC は正常資産のみで containment を 1 本も踏まず full-green
- 原因: エンジン流用時に「エンジンの入力信頼前提」を確認しなかった。paste 経路の入力 = ユーザーが今貼った内容（自己責任・絶対パス import は意図された機能）に対し、新経路の入力 = ディスク上に既在の md 本文（インポート/同期/共有由来 = 攻撃者制御になりうる）で、信頼境界が変わるのに防御を足さなかった。「共有防御を一部経路にだけ配線」の逆パターン（無防御エンジンを新しい信頼境界へ配線）
- 回避策: (a) **既存エンジン/正典を新しい呼び出し面へ配線するときは、エンジンの入力信頼前提（誰が内容を制御するか）が新経路でも成立するかを 1 度明示的に問う**。ディスク上のファイル本文・URL・外部データを流すなら containment（isUnderNoteDir / safeResolveUnderDir 系）を opt-in で足す（既存経路の pin を壊さないため無条件変更でなくオプション）。(b) 同一ファイル内に同種処理（資産複製）が複数あるとき、防御条件（絶対パス skip / `../` skip）の差分を突き合わせて揃える。(c) ファイル本文由来の参照を fs 解決する新機能の番人 TC は「絶対パス + `../` escape の非複製」を counterfactual で必ず 1 本踏む（正常資産のみの TC は containment 欠落を検出できない）

## 2026-08-22 [20260822-051129-fv-cleanup-viewer-find-recent-batch] 「全成功なら削除」ゲートの成功判定を、失敗を swallow して成功値を返す既存ヘルパ（makeUniqueImageCopier）の上に記録して画像経路だけ迂回を作った + webview 文言キーを消費側（__outlinerMessages=WebviewMessages）でなく host 側 Messages interface に登録して全 locale 英語固定
- 現象: (A) NFR-ACD-01 の report.copied を copyImg（copy 失敗を catch {} で握って destAbs を返す既存ヘルパ）の戻りだけ見て push したため、画像の copy 失敗が copyFailed に落ちず「1 件でも失敗なら削除しない」ゲートを画像だけ迂回 — source 画像を trash して dest に実体なし（データロス経路）。files/drawio ループは明示 catch 済みで非対称。既存 TC は files 失敗しか踏まず full-green。(B) viewerFind を Messages（host t() 用）に登録したが、file-viewer の label() は window.__outlinerMessages（= WebviewMessages / webviewMessages ブロック由来）を読む — note/sidepanel 面で全 locale 英語固定。既存 i18n 番人はキーの存在だけ見て interface 帰属を見ないためすり抜け
- 原因: (A) 成功判定を組む際、依拠する既存ヘルパの**例外契約（throw するか swallow して成功値を返すか）**を確認しなかった（2026-08-18「precedent 関数の例外契約を確認せず合流」の成功判定版）。(B) i18n が Messages / WebviewMessages の 2 interface 構造であることを忘れ、直近 sprint で使った Messages 側の追記手順をコピーした（消費側 = label()/t() のどちらが読むかを確認しなかった）
- 回避策: (a) **「全成功なら X」ゲートの成功記録は、依拠する複製/書込ヘルパが swallow 型でないか必ず確認**し、swallow 型なら成否を実在検証（existsSync 等）してから記録する（opt-in 経路のみに限定すれば既定 byte 不変と両立）。番人 TC は「ヘルパが swallow する種類の失敗」（dest 側 EACCES）で counterfactual を踏む。(b) **webview 文言キーは消費関数（label()=__outlinerMessages=WebviewMessages / deps.t()=Messages）を先に確認して帰属を決める**。i18n 番人はキー存在でなく interface 帰属ブロックまで grep する

## 2026-08-23 (viewer-office-text-image) — 「N kind の一部にだけ配線」再発: エラー整形の非対称
- 同一基盤（viewer-common/zip.mjs）を消費する 3 kind（docx/xlsx/pptx）のうち、docx/xlsx にだけ zip エラー → 友好的文言（viewerProtectedFile/viewerTooLargeToRender）変換を実装し、pptx は素通し（未ローカライズ技術メッセージが UI に露出）。reviewer finder が検出（QUAL-1 → TASK-18）
- 教訓: 「共通基盤のエラーを kind 側で整形する」パターンは、**kind 追加のたびに全 kind へ同時適用**。testcases 設計時に kind×共通挙動のマトリクス（§H 型）へ「エラー整形」行を入れると漏れが構造的に見える
