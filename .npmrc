# pdfjs-dist の optionalDependencies (@napi-rs/canvas = native module) を排除する。
# テキスト抽出 (getTextContent) は canvas 不要で、native module を入れると
# platform 別 vsix が必要になる (ADRL-0057 / poc BH-02 実測: --omit=optional で node-gyp 0)。
omit=optional
